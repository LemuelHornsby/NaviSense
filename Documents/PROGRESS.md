# NaviSense UE5 — Progress Ledger

One line per work packet. Newest first. Format:
`<date> · WP-n · <theme> · gates <passed>/<total> · <next blocker / note>`

Weekly metrics (Mondays): packets closed, nightly green-rate, D-gate burndown, fps headline, evidence-pack time.

---

## Demo gate burndown (target WAS 11 Jul 2026 — SLIPPED; live session pending, first free slot)

> **14 Jul (WP_20260714):** evidence side is now DONE. The KI-038 stale view HEALED on the fresh 14-Jul mount, the run-125800 pack was rebuilt **COMPLETE** (`view_complete: true`, 23,393/23,393 rows, health 6/6, AIS monaco_capture 3 targets/2 alerts) and the full-view re-proof is green (suite 4/4 on 639/639 envelopes · fidelity 6/6, resid 0.01 m); `runs.csv` row present. **Remaining = ONE ~60-min in-engine sitting** (D2 SS5 eye-word · D6 stills · D7 film · COLREGS live matrix · TC-49 closeout · KI-034 upright-word) **+ a separate ~30-min D8 clean-box pass** — follow `Documents/NaviSense_UE5_Gate_Closeout_Guide.md` (supersedes the step list in `PENDING_EDITOR_GATES.md`, which now points at it).
>
> **14 Jul (WP_20260714B, scheduled run — verify + closeout-tooling fixes):** re-ran the objective closeout verifiers against Session A's LIVE runs and found+FIXED two tooling bugs that would have blocked the closeout: **KI-041** — the D7 film gate's MP4 parser read only the first 4 MB, so it **false-rejected every moov-at-end recording** (Game Bar/editor default); the 3 real clips now parse (Monaco 96.5 s, Turning_Circle 416 s) and **C3 film PASSES**. **KI-042** — `verify_colregs.matrix()` **crashed** on a NUL-padded result file (mount, KI-038 family); now recovers. `verify_20260714b` 4/4+3/3 (TC-52). Verified on disk: D2 SS5 run 155234 health **8/8**, sensor suite **4/4**, COLREGS matrix **4/4** (head_on LIVE today; crossing-giveway/standon/overtaking still **9-Jul headless**). **UPDATE (20:07, same session): Lemuel completed ALL of Session A live** — 28 beauty stills captured (18:28), all 4 COLREGS scenarios run live (18:29–19:05), `verify_demo_session` = **SESSION PASS** (sensors 4/4 · capture 3/3 · COLREGS 4/4 live; TC-49). **Demo closeout DONE — burndown 7✅ / 1◐ (only D8 clean-box remains).** The KI-041 film fix was on the critical path (C3 would otherwise have blocked SESSION PASS).** Test-tooling only — no C++/wire/rebuild.

D1 ✅ closed loop LIVE + **sign CONFIRMED** (G7 starboard, 21 Jun) + **KI-018 spin RESOLVED** (493° continuous turn post-rebuild, 8/8) · D2 ✅ 6-DOF water ride — **CLOSED 14 Jul (Session A): Lemuel eye-confirm on SS5 run `unreal-test-run_20260714_155234` (mmg, 12,732 rows) — hull rolls/pitches/heaves with the swell while settled at the boot-top, props submerged (G_UE7/G_UE8+)** — trail: **heel/heave/wave roll+pitch CONFIRMED in-engine 21 Jun** (run 164358 SS5: roll ±6°/pitch ±4°/heave ±2.5°, plant 8/8; SS0 flat); only rendered water-surface sampling (F1 pt3) remains; **24 Jun: DA_DOLPHIN_HydrostaticsConfig + VesselProfile assigned**; **G_HYDRO float CONFIRMED in-engine 28 Jun** (Lemuel: `WaterlineOffsetCm=-218`, hull settles at the boot-top, props+bow-thruster submerged; hydrostatics samples the rendered `WaterBodyOcean` ⇒ F1 pt3 rendered-surface sampling effectively done; remaining D2 = a quick SS5 wave-ride re-check with hydrostatics active; 12-Jul live run rode SS2 for 13 min; 14-Jul SS5 run confirmed by eye — G_UE7/8 CLOSED) · D3 ✅ ≥3 runtime sea states logged + **CONFIRMED in-engine 21 Jun** (building_sea_transit swept SS1→SS6: 6 transitions logged at t=0/15/45/75/105/150 s, heave RMS 0.14→1.33 m; Lemuel saw the smooth build) · D4 ✅(scoped) real sensors — **CLOSED 12 Jul PM on the live MMG run 125800: TC-23 GPS/IMU fidelity 6/6 + TC-43 AIS/radar/camera suite 4/4 on the wire (437 `sensor.v1` envelopes, 3 mmsi, 1,308 blips, frames monotonic); Cesium live-GPS scoped OUT (KI-014, sensor-suite roadmap, not demo-gating)** — trail: **GPS/IMU now objectively validated against the plant** (WP-20260622, `verify_sensors_fidelity.py`: speed/yaw-rate/heading corr 1.0000, position median residual 0.18–0.43 m, WGS84 geo-origin consistent; runs 20260622_054815 + 21-Jun 360° both 8/8, 5/5 neg-controls); remaining: camera (WP-14), live CesiumGeoreference, and UE AIS-pawn rendering (WP-15B); **24 Jun WP-20260624: AIS traffic DATA/ANALYSIS half delivered headless** — scripted target with correct range/bearing + CPA/TCPA + COLREGS encounter in the evidence pack (`verify_20260624` 6/6 + 3/3); **27 Jun WP-20260627: COLREGS conformance SCORING added** — `python/colregs_score.py` returns a per-encounter give-way/stand-on verdict (Rules 8/13–17) into the evidence pack (`verify_20260627` 6/6 + 3/3), validated both ways (a compliant give-way scores COMPLIANT; a held course / wrong-way turn is flagged); seeds the §5.1 week-5-6 V&V differentiator; **1 Jul WP-20260701B: OWN-SHIP AIS receiver feed wired on `sensor.v1`** (`ais.targets[]`, was empty; `verify_20260701b` 5/5+3/3, geometry parity 0.00e+00); **1 Jul WP-20260701C: CAMERA sensor (WP-14) wired on `sensor.v1`** via the low-risk still-frame/metadata approach — `camera{fovDeg,resX,resY,headingDeg,frameIndex,frameRef,pose}` + reusable `python/camera_sensor.py`, `frameRef` names the WP-20260630 HighResShot stills, `verify_20260701c` 5/5+3/3, `Z0` 16/16; camera item now on the wire (pixels via the existing HighResShot capture); scoped-out: live `CesiumGeoreference` GPS (deferred behind the OPEN S2 KI-014 de-risk); **G_AIS_SENSOR_UE/G_RADAR_UE/G_CAMERA_UE closed in-engine 8 Jul (run 195058) and RE-PROVED 12 Jul on the live MMG run; 14 Jul full-view re-proof: suite 4/4 (639/639 envelopes, 1,914 blips) + fidelity 6/6 (resid 0.01 m)** · D5 ◐→✅(mechanic) wake VFX — **G_WAKE_UE CONFIRMED in-engine 28 Jun** (Lemuel): `WakeViz` Niagara on `BP_ShipPawn_Yacht`, Event Tick binds `User.WakeIntensity=GetWakeIntensity01`/`User.Spray=GetWakeSpray01`; speed-driven white **foam trail** (Local Space world-space + tuned velocity 20-80 / gravity 0 / lifetime 4-6 s), thins at rest. Final cinematic polish folds into D7. **WP-20260628**: speed-driven feed authored — curve `python/wake_model.py` + pawn `GetWakeIntensity01/Spray01` (BlueprintPure) + `04_setup_wake_vfx.py` + `Documents/NaviSense_Wake_VFX_Recipe.md`; `verify_20260628` **6/6 + 3/3** with **C++↔Python curve parity** (the rendered curve == the gated curve); remaining = build the NS_Wake Niagara system + in-engine eye-check (**G_WAKE_UE**) · D6 ✅ **evidence-pack generator + IMO-KPI bundle** (WP-20260620: `build_evidence_pack.py` -> `logs/<run>/evidence_pack/`); **scenario selection DONE + confirmed in-engine 21 Jun** (`--scenario building_sea_transit`/`imo_turning_circle` ran live; evidence pack from run 163148 = DT/Lpp 4.16 IMO PASS); **one-command runner DONE headless 23 Jun** (`run_demo.py`, verify 5/5+3/3); **single-file HTML evidence report DONE headless 26 Jun** (WP-20260626: `evidence_report.html` — plots embedded as base64, IMO-KPI table + health verdict + AIS/COLREGS, honest provenance, verify 6/6+3/3); **12–13 Jul: pack ran on the LIVE run 125800 — but the 12-Jul artifact was silently built from a stale 47% sandbox view (KI-038; Windows TC-17 saw all 23,393 rows); 13 Jul P0 view+manifest-integrity gate shipped (TC-51 6/6+2/2) and the on-disk pack rebuilt watermarked PARTIAL — a ≤2-min Windows rebuild makes it the real D6 artifact**; **14 Jul: the stale view HEALED on a fresh mount and the pack was rebuilt COMPLETE (`view_complete: true`, 23,393/23,393 rows, health 6/6, AIS 3 targets/2 alerts, plots+HTML) — the real D6 run artifact is ON DISK**; ≥3 beauty screenshots **DONE 14 Jul: 28 HighResShot 3840×2160 stills captured 18:28 ⇒ capture gate 3/3, D6 CLOSED**; **4 Jul (WP-20260704): headless demo-readiness rehearsal `demo_rehearsal.py` — the 4-scenario demo storyline scores DEMO READY 4/4 (D1/D2/D3/D4/D6 covered headless), gated by `verify_20260704` 5/5+3/3** · D7 ✅ film — **CLIPS CAPTURED in-engine 14 Jul (Session A)** in `Demo/film` (Turning_Circle ~7 min · Monaco_capture 96 s · Head_on); **G_FILM_UE / C3 now PASSES** (`verify_capture_artifacts --film-dir` — 2 clips parse; the pre-14-Jul parser false-rejected them, **KI-041 FIXED** WP_20260714B). **CLOSED 14 Jul: `verify_demo_session` SESSION PASS (TC-49)** — screen-recorded demo clips (MRQ cinematic is post-demo polish, KI-019 honesty) · D8 ◐ clean-machine repro — **WP-20260625**: `python/repro_doctor.py` (stdlib-only 12-check readiness verdict: deps / core-tool parse / UE project / DA_DOLPHIN assets incl. un-pulled Git-LFS stubs / Cesium token), `SETUP.md` documented clone→demo path, and `verify_20260625.py` **6/6 + 3/3** proving the headless pipeline reproduces (`run_demo --selftest` ×2 → DT **158.18 m IMO PASS** both); remaining = one-time clean-box **in-engine** confirm (G_REPRO_UE) + inherits D2/D5/D7

**Sensor Suite Roadmap (NEW scope beyond D1–D8, directed 1 Jul 2026):** Radar/LiDAR/Sonar are net-new sensors that are NOT part of any demo gate — they must not block the 11 Jul demo or divert D4 effort. **Radar ✅ shipped headless 2 Jul (WP-20260702)** — anonymous blips on `sensor.v1 radar.contacts[]`, `verify_20260702` 5/5+3/3, in-engine G_RADAR_UE pending rebuild. **LiDAR / Sonar ☐** design-recorded in `Documents/NaviSense_Sensor_Suite_Roadmap.md` (§5), not yet implemented (Sonar needs a seabed mesh decision first). Honesty: geometric/derived models, not validated physics (KI-027 for radar).

## GTM metrics (T1–T5) — baseline 2026-06-13 · updated 2026-06-19 (W2)

| Metric | Baseline (13 Jun) | Current (19 Jun, W2) | Target (2 Oct) | Notes |
|---|---|---|---|---|
| T1 Audience (LI + YT + waitlist) | 0 | 0 reported — **handle reservation unconfirmed** | 500 | Not measurable until a handle is live; Lemuel to report |
| T2 Community (installs + Discord) | 0 | 0 (pre-launch) | 150 installs / 75 Discord | Public repo opens 25 Aug |
| T3 Pipeline (qualified conversations) | 0 | 0 held; warm list **0/10 filled** | 12 | First outreach 11 Jul; fill warm contacts this week |
| T4 Revenue (proposals sent / signed) | 0 / 0 | 0 / 0 | 6 sent / 2 signed | Pilot one-pager due W5 (12 Jul) |
| T5 Non-dilutive (grant apps submitted) | 0 | 0 submitted — DIANA v2 ready (submit 30 Jun); Inception **unconfirmed**; Epic + Cesium **verified & fitting** | 3 | DIANA on track; 2 new programs added to the stack this week |

*Honesty: nothing is published or submitted yet, so most metrics sit at the pre-launch baseline. W2 movement is prep-side — DIANA v2 done, two extra grants verified. T1 and the Inception status need Lemuel to report; the session cannot read them.*

**GTM session note — 2026-06-19 (TP_20260619, W2):** W2 packet prepared (DIANA v2 + internal review, landing page copy, LinkedIn warm-up v2, grant verifications, soft-launch checklist). W2 gate met (DIANA v2 done; landing page staged). Found + corrected an unsupportable "CFD-validated / matches model-test data" claim → **KI-019**. PIPELINE.md refreshed (no zombie rows).

## GTM metrics (T1–T5) — updated 2026-06-26 (W3)

| Metric | W2 (19 Jun) | Current (26 Jun, W3) | Target (2 Oct) | Notes |
|---|---|---|---|---|
| T1 Audience (LI + YT + waitlist) | 0 reported | **0 reported — handle still unconfirmed to session** | 500 | Not measurable until a handle is live; Lemuel to report. Unblocks at domain/handle reservation. |
| T2 Community (installs + Discord) | 0 | 0 (pre-launch) | 150 installs / 75 Discord | Public repo opens 25 Aug. |
| T3 Pipeline (qualified conversations) | 0 held; warm 0/10 | 0 held; warm list **still 0/10** to session | 12 | First outreach 11 Jul. Eurostars partner shortlist (5 labs) prepared as partner-audition seeds. |
| T4 Revenue (proposals sent / signed) | 0 / 0 | 0 / 0 | 6 sent / 2 signed | Pilot one-pager due W5 (12 Jul). |
| T5 Non-dilutive (grant apps submitted) | 0 submitted | **0 submitted; pipeline thicker** — DIANA v3 review-ready (submit 30 Jun); Epic MegaGrants draft done (submit ~13 Jul); Eurostars shortlist done (due 10 Sep); Inception **still unconfirmed** | 3 | Three programs now have prepared drafts/shortlists; "submitted" count moves only when Lemuel submits. |

*Honesty: metrics still sit at the pre-launch baseline — nothing is published or submitted yet. W3 movement is prep-side: DIANA upgraded to v3 with real evidence + final proof pass, Eurostars partner shortlist delivered, Epic MegaGrants draft delivered. T1 and the Inception status need Lemuel to report; the session cannot read them.*

**GTM session note — 2026-06-26 (TP_20260626, W3):** W3 packet prepared (DIANA v3 + submission checklist, Eurostars partner shortlist, Epic MegaGrants draft, build-in-public LinkedIn warm-up post, soft-launch readiness). **W3 gate = "DIANA submitted"** — proposal is review-ready; the only blockers to the submit are Lemuel's two fills (§4 CFD, §5 bio) + three eligibility confirms. DIANA draft upgraded honestly using real artifacts logged 21–26 Jun (in-engine IMO turning-circle KPIs A/Lpp 4.09 + DT/Lpp 4.16 PASS, validated GPS/IMU, AIS/COLREGS encounter analysis, single-file evidence report, clean-machine repro) — several claims moved from "in build" to "working now." No new fabrication; KI-019 honesty framing preserved (MMG standard-method, CFD underway). PIPELINE.md refreshed (no zombie rows). No new QA issue surfaced from GTM this week.

## GTM metrics (T1–T5) — updated 2026-07-04 (W4, session ran Saturday)

| Metric | W3 (26 Jun) | Current (4 Jul, W4) | Target (2 Oct) | Notes |
|---|---|---|---|---|
| T1 Audience (LI + YT + waitlist) | 0 reported | **0 reported — handle still unconfirmed to session, 3 weeks running** | 500 | Not measurable until a handle is live; Lemuel to report. |
| T2 Community (installs + Discord) | 0 | 0 (pre-launch) | 150 installs / 75 Discord | Public repo opens 25 Aug. |
| T3 Pipeline (qualified conversations) | 0 held; warm 0/10 | 0 held; warm list **still 0/10 — now 3 sessions overdue** (18 Jun -> 25 Jun -> 3 Jul target all missed) | 12 | First outreach 11 Jul; this is now the single most overdue GTM item. |
| T4 Revenue (proposals sent / signed) | 0 / 0 | 0 / 0 | 6 sent / 2 signed | Pilot one-pager due W5 (12 Jul); unchanged. |
| T5 Non-dilutive (grant apps submitted) | 0 submitted | **0 submitted (unconfirmed); DIANA deadline (3 Jul 12:00 UTC) has PASSED with no submission record in this repo** — status unknown, awaiting Lemuel confirmation; Epic MegaGrants draft ready (target ~13 Jul); Eurostars shortlist ready (due 10 Sep); Inception **still unconfirmed, 4 weeks running**, now redirected as this week's non-dilutive priority since DIANA's window is shut either way | 3 | Honesty: cannot mark DIANA submitted without Lemuel's confirmation — see PIPELINE.md. |

*Honesty: metrics still sit at the pre-launch baseline — nothing is confirmed published or submitted. W4 movement is a risk flag, not progress: the DIANA deadline passed without a recorded submission, and the warm-contact list + handle reservation remain unconfirmed for 3+ weeks running. This session could not read the DIANA portal, LinkedIn/YouTube account state, or Lemuel's private task completion — all three T1/T5 gaps are informational, not code gaps.*

**GTM session note — 2026-07-04 (TP_20260704, W4, ran Saturday — no Friday session occurred):** No Friday session ran this week; this session covers W4 a day late. **W3 gate ("DIANA submitted") status UNKNOWN/likely MISSED** — the 3 Jul 12:00 UTC deadline passed with no submission confirmation anywhere in the repo since the 26 Jun review; flagged to Lemuel as the top action item, with non-dilutive effort redirected to NVIDIA Inception (still open) per the calendar's "adapt, don't guilt-stack" rule, since the DIANA window itself cannot be re-opened. **This week's gate ("soft-launch kit ready by 10 Jul") is at risk**: no film capture or beauty screenshots exist yet (D6/D7 still open), and 27 Jun-3 Jul dev sessions went into the sensor suite (radar/camera-metadata/dashboard) and a new COLREGS scripted-encounter + compliance-scoring feature (WP-20260703, real and well-verified) rather than D6/D7 capture work — flagged as a build-priority risk to the 11 Jul date in the weekly brief. Prepared: a new build-in-public LinkedIn post (`LinkedIn_COLREGS_scoring_post.md`) drawn honestly from the 3 Jul COLREGS scoring run (four scored encounters, real miss distances, KI-028 honesty caveat preserved); refreshed soft-launch critical path (`soft_launch_critical_path.md`). PIPELINE.md refreshed: DIANA row marked deadline-passed/status-unknown, warm-contacts row marked overdue with updated date, Inception row redirected as this week's priority — no zombie rows. No new QA/build defect surfaced from the GTM review itself (the capture-readiness gap is already tracked under D6/D7 in the burndown above, not a new issue).

## GTM metrics (T1–T5) — updated 2026-07-11 (soft-launch day itself, session ran Saturday)

| Metric | W4 (4 Jul) | Current (11 Jul) | Target (2 Oct) | Notes |
|---|---|---|---|---|
| T1 Audience (LI + YT + waitlist) | 0 reported | **0 reported — handle still unconfirmed to session, 4 weeks running** | 500 | Not measurable until a handle is live; Lemuel to report. |
| T2 Community (installs + Discord) | 0 | 0 (pre-launch) | 150 installs / 75 Discord | Public repo opens 25 Aug — unchanged, no action expected yet. |
| T3 Pipeline (qualified conversations) | 0 held; warm 0/10 | 0 held; warm list **still 0/10 — now 4 sessions overdue, and today is the soft-launch date itself** | 12 | This is now the single highest-leverage overdue item in the whole plan — see PIPELINE.md. |
| T4 Revenue (proposals sent / signed) | 0 / 0 | 0 / 0 — **design-partner one-pager drafted this session** (`TP_20260711/04_design_partner_one_pager.md`), pulled forward from the 12 Jul (W5) gate | 6 sent / 2 signed | No conversations open yet to send it to; drafted so it's ready the moment one opens. |
| T5 Non-dilutive (grant apps submitted) | 0 submitted (DIANA unknown) | **0 confirmed submitted.** DIANA status still unconfirmed (deadline passed 8 days ago); Inception still unconfirmed (5 weeks running); Eurostars shortlist + Epic MegaGrants draft ready and waiting on the film; Cesium/DASA monitored, no action due | 3 | Consolidated in `TP_20260711/05_grant_status_and_asks.md` with exact yes/no questions for Lemuel. |

*Honesty: today (11 Jul) is the Traction Calendar's own soft-launch date, and the checked-not-assumed repo state is that the film/stills still don't exist (`verify_capture_artifacts` FAIL, last run 9 Jul: 0 stills, 0 clips) and the warm-contact list is still empty. This is a real, dated gate miss, not a rounding error — recorded here rather than smoothed over.*

**GTM session note — 2026-07-11 (TP_20260711, soft-launch day, ran Saturday — no Friday session occurred this week either):** Verified directly against the repo (not memory) that the soft-launch kit is not ready: no film, no beauty stills, warm-contact list still 0/10, domain/handle still unconfirmed. Per the calendar's "adapt, don't guilt-stack" rule, this session does not draft a launch post that promises a film that doesn't exist. Prepared instead: `01_soft_launch_adaptation.md` (two honest paths — delay the film-led launch a few days vs. post a text-led story today — recommended: delay, since a promised-but-missing film undercuts the "evidence not claims" pitch); a no-film LinkedIn draft and a fill-in-the-blank film-led draft ready the moment the capture session runs; the design-partner one-pager pulled forward from its 12 Jul gate (not blocked on the film); and a consolidated grant-status doc with exact yes/no questions for Lemuel (DIANA, Inception). PIPELINE.md refreshed: warm-contacts row updated (now blocking the actual launch, not just a calendar date), two new trackable rows added (design-partner pilot, Eurostars partner) so they don't become untracked someday-items, grant tracker dates refreshed — no zombie rows. **No new QA/build defect surfaced from the GTM review itself** — the missing film/stills are already tracked under D6/D7 in the burndown above; this session's finding is a schedule/prioritization fact, not a new code or product defect, so no new KI was opened.


## Weekly dev metrics — Mon 2026-07-06 (Week 5) — **T-5 to demo**

- Packets closed (auto gates): **WP-20260630** (capture-artifacts verify) · **WP-20260701** (dashboard data+control layer: 19 BlueprintPure getters + SetHelm/SetThrottle/SetBowThruster + Build.cs UMG/Slate/SlateCore) · **WP-20260701B** (AIS `sensor.v1 ais.targets[]` receiver) · **WP-20260701C** (camera `sensor.v1 camera{}` still block) · **WP-20260702** (radar `radar.contacts[]` blips) · **WP-20260702B** (stacked pre-rebuild link audit) · **WP-20260703** (single-target COLREGS encounters + scripted avoidance) · **WP-20260705** (demo GO/NO-GO preflight) · **WP-20260706** (this: T-5 hold-the-line + Monday metrics). TP-20260704 was the W4 GTM/traction packet. **All headless-green; every in-engine gate is stacked behind the ONE shared rebuild.**
- Nightly green-rate: **n/a** (Task Scheduler nightly still not wired to a build). Objective gate set today on-disk: preflight **GO** (Z0 16/16 · stacked link audit `verify_20260702b` 5/5+3/3 · `demo_rehearsal` DEMO READY, fresh 06 Jul, health 7/7, COLREGS COMPLIANT) + `verify_20260704` 5/5+3/3. `verify_20260706` PASS 2/2 → `wp_20260706_result.json`.
- D-gate burndown (unchanged — **no in-engine session ran this week**): **D1 ✅ · D3 ✅ · D5 ✅(mechanic, 28 Jun)** · **D2 ◐** (SS5 wave-ride re-check, hydrostatics active) · **D4 ◐→ mostly ✅** (GPS/IMU validated; **AIS/radar/camera CONFIRMED in-engine 8 Jul** — `verify_sensor_suite` 4/4 on run 195058; only Cesium GPS deferred behind KI-014) · **D6 ◐** (evidence pack + scenario select done; beauty stills pending capture) · **D7 ☐** (MRQ film — needs the capture session) · **D8 ◐** (repro doctor). **Critical path: the ONE rebuild + PIE/capture session unlocks D2, D4, D6, D7 at once — now 5 days out.**
- FPS headline: **~40 fps** (Lemuel's in-engine NaviSense_Monaco, Cesium 3D Tiles present) — unchanged; no new in-engine perf snapshot this (headless) session.
- Evidence-pack time (D6): operational — `run_demo --selftest` builds the full pack + single-file HTML report headless in tens of seconds (last e2e 27.0 s).
- Top risk this week is **schedule, not code**: the headless tree has been GO/rebuild-safe for days; the single demo-critical action (Step 0 rebuild → Steps 1-3 PIE/capture) has not yet been run and now has 5 days of slack. Also open: **KI-006** (off-machine git remote, S1), **KI-014** (Cesium load, S2), **KI-012** (deterministic engine sim-clock).

## Weekly dev metrics — Mon 2026-06-29 (Week 4)

- Packets closed (auto gates): **WP-20260629** (canonical run-clock `t_mono`, **closes KI-024**) — `verify_20260629.py` 6/6 + 3/3, no in-engine gate. The prior week's headless packets (WP-20260623 runner … WP-20260628 wake feed) all closed; **WP-20260628 wake mechanic + D2 float + KI-021 manual drive were also CONFIRMED in-engine 28 Jun**.
- Nightly green-rate: **n/a** (Task Scheduler nightly still not wired to a build). The objective gate set grew again: `verify_20260629` (canonical-clock, exit 0/1) now joins `verify_run_kinematics` (K1–K8) + `verify_sensors_fidelity` (D1–D8) as nightly-ready checks.
- D-gate burndown: **D1 ✅ · D3 ✅** · **D2 ◐** (G_HYDRO float confirmed in-engine 28 Jun; remaining = SS5 wave-ride re-check with hydrostatics active) · **D4 ◐** (GPS/IMU validated; **KI-024 data-integrity bug RESOLVED this session** — canonical `t_mono` join; remaining = camera WP-14, live CesiumGeoreference, UE AIS-pawn WP-15B) · **D5 ◐** (wake mechanic confirmed in-engine 28 Jun; cinematic polish folds into D7) · **D6 ◐** (evidence pack + HTML report + one-command runner done; remaining = ≥3 in-PIE beauty screenshots / MRQ) · **D7 ☐** MRQ film · **D8 ◐** (headless repro proven + SETUP.md; remaining = clean-box in-engine confirm). Net: 2 closed, 5 ◐, 1 ☐.
- FPS headline: **~40 fps** (Lemuel's in-engine NaviSense_Monaco sessions, Cesium 3D Tiles present) — unchanged; no new in-engine perf snapshot this (headless) session.
- Evidence-pack time (D6): operational — `run_demo --selftest` builds the full pack + single-file HTML report headless in tens of seconds; the e2e gate ran in **27.0 s**.
- Open functional items after KI-024: **KI-014** (Cesium load — likely resolved 21 Jun, awaits a clean confirm), **KI-012** (deterministic engine sim-clock — in-engine), **KI-006** (off-machine git remote — Lemuel, S1). Biggest near-term value is now **in-engine capture** (D6 screenshots, D7 film, D2 SS5 re-check) — all need Lemuel + PIE; the headless frontier stays largely exhausted.

## Weekly dev metrics — Mon 2026-06-22 (Week 3)

- Packets closed (auto gates): WP-20260620 (kinematic health gate), WP-20260621 (sea-state schedule), **WP-20260622 (sensor-fidelity gate — closed; no in-engine gate)**; WP-20260621_HYDRO is headless-green (9/9) with its in-engine gates still pending Lemuel's full rebuild + config asset + Blender prop split.
- Nightly green-rate: **n/a** (Task Scheduler nightly still not wired to a build) — but the objective gate set keeps growing: `verify_run_kinematics` (K1–K8) + **new** `verify_sensors_fidelity` (D1–D8), both exit 0/1 for the nightly.
- D-gate burndown: **D1 ✅ · D3 ✅** (both confirmed in-engine 21 Jun) · **D2 ◐** (only rendered water-surface sampling left; HYDRO authored, rebuild pending) · **D4 ☐→◐ this week** (GPS/IMU validated vs plant) · D6 ◐ · D5/D7/D8 ☐.
- FPS headline: **~40 fps** (Lemuel's 21-Jun in-engine session, NaviSense_Monaco, Cesium 3D Tiles present).
- Evidence-pack time (D6): operational — `build_evidence_pack.py` produced the IMO KPI bundle on run `163148` (DT/Lpp 4.16 PASS) in seconds, headless.
- New finding: **KI-024** — the plant-log and sensor-log use different `t` clocks (3.0× vs 1.0× wall on the morning run); join on `wall_time`.

## Weekly dev metrics — Mon 2026-06-15 (Week 1)

- Packets *fully* closed (incl. in-engine gates): **0** — WP-3/4/5/6 + SENSOR-1 + ACTUATOR-RIG all still await Lemuel's in-engine confirmation (now unblocked — see KI-015).
- Nightly green-rate: **n/a** — nightly automation not yet run on a build (WP-5 G_UE pending).
- D-gate burndown: **D1 in final sign-confirm; D2–D8 open** (no change this week).
- FPS headline: **not yet measured in-engine** (no PIE perf snapshot captured).
- Evidence-pack time (D6): **n/a** (scenario pack not yet built).
- ⚠️ Integrity: 5 C++ files were truncated on disk (KI-015) and fixed today — the recompile that gates the whole editor queue is now actually compilable.

## ▶ Next packet (directed by Lemuel, 1 Jul 2026 — supersedes the 30 Jun dashboard callout)

**Full sensor suite integration (incl. Radar/LiDAR/Sonar) + finish the Bridge Dashboard UI.** Full
spec + recommended sequencing: `Development/work_packets/NEXT_PACKET_DIRECTIVE.md`. Three parts: (1)
finish `WBP_BridgeDashboard` (layout done, 1/19 getters bound as of 1 Jul — mostly manual/in-editor);
(2) close the concrete **D4** sensor gaps — ~~AIS mmsi/cog/sog into `sensor.v1`~~ **DONE 1 Jul
(WP-20260701B, headless 5/5; in-engine `G_AIS_SENSOR_UE` pending the rebuild)**; still open: GPS via
a live `CesiumGeoreference` (flat-earth today) + Camera capture (WP-14, never started); (3) **Radar/LiDAR/Sonar are net-new scope
beyond the original D4 gate** — valuable roadmap work but NOT required for the 11 Jul demo; each
needs a design-decisions pass (range/FOV, scan rate, noise model, wire format, perf budget) before any
code. WP-20260701 (30 Jun dashboard directive) shipped its headless half 1 Jul — see the Ledger.

## Ledger

- 2026-07-14 · WP_20260714C · **Off-machine git backup ENABLED (KI-006 top risk → IN PROGRESS).** Settled the full remote-backup plan for `github.com/LemuelHornsby/NaviSense`: scope = code+docs+UE **Content**+ship models via LFS (~3 GB — fits GitHub's **free 10 GiB** LFS storage, quota re-verified current), demo films (2.4 GB) excluded (HDD/cloud). Found+fixed a real push blocker: `dolphin.3dm` (339 MB) + `.3dmbak` weren't LFS-tracked ⇒ GitHub's **100 MB non-LFS limit** would have failed the push — added `*.3dm`+CAD/texture/archive types to `.gitattributes`, git-ignored `*.3dmbak` + `Demo/film/`. Rewrote `GIT_SETUP.md` into a complete Windows runbook (fresh history, **`--force` replace** the dated repo, pre-push >95 MB LFS audit, post-push verify + default-branch tidy). Sandbox can't run git on D: — **Lemuel executes it; KI-006 closes on his confirmed push.**
- 2026-07-14 · WP_20260714B closeout CONFIRMED · **Session A COMPLETE ⇒ demo closeout DONE (7✅ / 1◐).** After the KI-041/KI-042 tooling fixes, Lemuel ran the rest live: **28 HighResShot 3840×2160 stills** (18:28) ⇒ capture gate **3/3** (C1 28 stills · C2 run 155234 health 8/8 · C3 **5 clips** — the KI-041 fix); **all 4 COLREGS scenarios live** (head_on 182748 · crossing-giveway 184900 · crossing-standon 185359 · overtaking 190057) ⇒ `verify_colregs --matrix` **4/4 live** (KI-042 fix kept it from crashing on the padded head_on file); **`verify_demo_session` = SESSION PASS** (TC-49, `demo_session_result.json`). Burndown now **D1–D7 ✅ · D8 ◐** (clean-box Session B ~30 min, any day). D2/KI-034 were Lemuel eye-confirmed earlier today.
- 2026-07-14 · WP_20260714B (scheduled, headless) · **Closeout-tooling hardening — KI-041 + KI-042 found+FIXED** while re-running the objective closeout verifiers against Session A's live runs. **KI-041 (S2):** `verify_capture_artifacts` film parser read only the first 4 MB, so it false-rejected every moov-at-end recording — the **D7 film gate could never pass** on the real clips; fixed (head+tail mvhd scan, sanity-bounded) ⇒ C3 now PASS (Monaco 96.5 s + Turning_Circle 416 s parse). **KI-042 (S3):** `verify_colregs.matrix()` crashed (`JSONDecodeError`) on a NUL-padded `colregs_head_on_result.json` (mount, KI-038 family); fixed (`_load_result` recovers padding / flags true corruption) ⇒ matrix 4/4, no crash. New `verify_20260714b` **4/4 + 3/3**, `TC-52`, `wp_20260714b_result.json`. Also verified on disk: D2 SS5 run 155234 health **8/8**, sensor suite **4/4** (run 153701). Test-tooling only — no C++/wire/rebuild. **TRUE remaining closeout = D6 ≥3 stills (still 0 on disk) + 3 LIVE COLREGS runs (crossing-giveway/standon/overtaking are 9-Jul headless) + TC-49 + D8 clean box.**
- 2026-07-14 · Session A (live, A4 debug) · **KI-039 found+RESOLVED — `run_colregs.py` wire-port default 5502 vs pawn 5005** (one-line Python fix; NO C++). First real `--head-on` attempt (stale listener cleared) left the bridge `RECONNECTING…`: runner listened on 5502 while `NaviSenseBridgeComponent.Port=5005`. Headless matrix never caught it — selftests stub both socket ends on the same port. Default now 5005 (`--port` still overrides); Troubleshooting + guide banner-check updated. Live retry queued.
- 2026-07-14 · Session A (live, A4 resume fix) · **Give-way ships now RESUME the base course after the pass** (Lemuel filmed the head-on and flagged the ship diverging forever): `alter_end_s` default 220 s sat beyond the ~2-min staged encounters — per-encounter resume now (head-on/crossing t=130 s, overtaking 170 s; existing ramp-back logic, values only). Selftests ×3: COMPLIANT, passes 222/214/295 m, end heading within ±6° of base — the demo now shows the full COLREGS arc: approach → starboard turn (t≈20 s, ~410 m) → 222 m port-to-port pass (t≈100 s) → resume (t≈130–150 s). Staging LOCKED on Lemuel's word.
- 2026-07-14 · Session A (live, A4 staging final) · **Head-on locked at the Rule-8 physics floor** after Lemuel's eye-feedback (turn later; boat read stationary at 2 kn): 500 m start · target 2.3 kn (visibly underway) · **turn t=20 s at ~410 m** · 55° cmd/20 s snappier ramp ⇒ **pass 222 m COMPLIANT (net 67° starboard)**, health PASS. Two hotter variants (176 m, 194 m) FAILED the 200-m gate and were rejected — turning later than ~400 m cannot make a compliant pass at these speeds; that IS the demo story.
- 2026-07-14 · Session A (live, A4 staging v2) · **Head-on staged tighter on Lemuel's eye-feedback** (turn confirmed 'works perfectly'; start still read far): 800→**500 m start**, target 2→1 m/s, turn t=15 s (~430 m), 45° cmd ⇒ **pass 227 m COMPLIANT** (net 51.1°, health PASS, selftest 173719). Crossing/overtaking/standon staging unchanged (213/295/152 m).
- 2026-07-14 · Session A (live, A4 staging) · **KI-040 RESOLVED in-engine** (run 170917: t=0 AIS 1,598 m/−1.1° after the Live Coding recompile) **+ COLREGS encounters re-staged for the camera, compliance intact** (pure Python: presets + controller constants). Lemuel's call: the 1,600 m Rule-8 early-action staging is correct COLREGS but unreadable on film. New staging keeps the 200-m safe-CPA scorer untouched and re-proves 4/4 headless: head-on 800 m start/2 m/s target, turn t=40 s (~580 m), **pass 236 m**; crossing 720 m start, turn t=18 s/45°, **pass 213 m**; standon 152 m; overtaking 295 m — all COMPLIANT, health PASS. The failed 900 m/85 m variant stays on record as the counter-example: staging may move, the scorer may not.
- 2026-07-14 · Session A (live, A4 debug #2) · **KI-040 found via the wire — AIS/radar sensor geometry mixed plant/world frames** (C++ fix authored; **Step 0 re-opened: ONE Live Coding recompile**, KI-034 pattern). Live head-on run 164648: `ais.targets` reported the boat at 255 m/123° at t=0 — exactly the preset (−30, 1600) viewed from the WORLD own-pose (spawn anchor −243.4E/+1738.2N); rendering anchors correctly (`bAnchorTrafficToSpawn`), so the boat looked ~1.6 km off and 'never approaching' while the time-scripted avoid still turned at t=30 s. Fix: `SensorBundleComponent` now expresses own-ship in the PLANT frame for AIS/radar (inverse of the KI-020 anchor; plant heading for relBearing); GPS/IMU untouched (fidelity contract). Z0 16/16. Camera-tune experiment: head_on 1600→900 m scored NON-compliant (85 m < 200 m safe CPA) → REVERTED, re-proof compliant (29.2° starboard, health 7/7). Preset stands at 1,600 m: the film shows a real Rule-14 early alteration, not a staged near-miss.
- 2026-07-14 · Session A (live, A2 done) · **D2 CLOSED** — Lemuel eye-confirm on SS5 `rough_turning_circle` run 155234 (`--plant mmg`, 12,732 rows, final): hull rides the swell at the boot-top (G_UE7/G_UE8+). SS5 film beat recorded and placed in `Demo/film` (800 MB, ~7 min). Burndown now **5✅ (D1/D2/D3/D4ˢ/D5) · 2◐ (D6/D8) · 1☐ (D7)**. Remaining: stills + beat-1 (fold into first COLREGS run) → A3–A5.
- 2026-07-14 · Session A (live, A1 done) · **TC-43 PASS on today's PIE run** `unreal-test-run_20260714_153701` (4/4 — 384/384 envelopes, 3 mmsi, 1,152 blips; view complete 7,252/7,252) + **KI-034 RESOLVED** (Lemuel eye-confirm: both roll-corrected hulls upright ⇒ **G_TRAFFIC_UE ✅**). Note: direct-listener scenarios default to `--plant stub` (only COLREGS scenarios force mmg) — guide A1/A2 commands updated to carry `--plant mmg` for demo-evidence runs. Film beat 1 not yet recorded — beats fold into A2 (SS5) + one COLREGS pass. Remaining in the sitting: A2→A5 + eye-word D2.
- 2026-07-14 · WP_20260714 · **Doc-current sweep + FULL-VIEW evidence pack + Gate-Closeout Guide** (interactive session; docs + evidence only, **NO C++/wire/DTO/schema/product change, NO rebuild**). KI-038 view **HEALED** on this session's fresh mount (`state.csv` 23,393/23,393 rows, `manifest.json` 1,343 B VALID, `runs.csv` row present) ⇒ run-125800 pack rebuilt **COMPLETE** sandbox-side with the P0 gate proving it (`view_complete: true`, health 6/6 on 23,393 rows, AIS monaco_capture 3 targets/2 alerts, plots + HTML) — the 'rebuild on Windows' human step is **CLEARED** (the live-run-packs-on-Windows rule stands for future runs). Full-view re-proof: **sensor suite 4/4** (639/639 envelopes, 3 mmsi, 1,914 blips) + **fidelity 6/6** (speed corr 1.0000, position resid 0.01 m, geo-origin 43.7350N/7.4250E). Authored **`Documents/NaviSense_UE5_Gate_Closeout_Guide.md`** — the single ordered runbook for everything still open: ONE ~60-min sitting closes **D2 (SS5 eye-word) · D6 (≥3 stills) · D7 (film, TC-42) · G_TRAFFIC_UE/KI-034 word · G_COLREGS_UE live matrix · TC-49 one-command closeout**, plus a separate ~30-min **D8 clean-box pass** (G_REPRO_UE); `PENDING_EDITOR_GATES.md` + CG/MP/AR banners updated to point at the guide. Burndown unchanged until the sitting: 4✅ (D1/D3/D4ˢ/D5) · 3◐ (D2/D6/D8) · 1☐ (D7).
- 2026-07-13 · WP_20260713 · **Post-live-session triage — the 12-Jul PM run is REAL but its evidence pack wasn't: P0 anti-partial-evidence gate** (pure Python + docs; **NO C++/wire/DTO/schema change, NO rebuild**). Found, not assumed: Lemuel ran the live session 12 Jul PM — `demo-monaco_capture_20260712_125800` (13 min MMG @ SS2, monaco_capture, 3 AIS targets): Windows-side TC-17 kinematics 6/6 on 23,393 rows, TC-23 GPS/IMU fidelity 6/6 (corr 1.0000), TC-43 sensor suite 4/4 ⇒ **D4 ✅(scoped)**, and KI-037 (cp1252 pack crash) was found+fixed live. BUT this sandbox sees a FROZEN view of that run dir ≥20 h later — `state.csv` 11,117/23,393 rows (47%), `manifest.json` truncated at 967 B (invalid JSON), no `runs.csv` row — and the 12-Jul on-disk pack had been silently built from that 47% view claiming PASS ⇒ **KI-038 (S2) opened**. Shipped: P0 view-completeness + manifest-integrity gate in `build_evidence_pack.py` (refuses exit 3 with a KI-038 message BEFORE writing; `--allow-partial` = clearly-watermarked forensic build; kpis now carry `view_complete`/`state_rows_read`/`state_rows_manifest` provenance), `python/verify_20260713.py` → **TC-51 6/6 gates + 2/2 neg-controls** (G5 reproduces the exact live loophole — a truncated manifest bypassed the naive row check on the first probe), on-disk 125800 pack deliberately rebuilt **watermarked PARTIAL**, baseline **TC-50 re-proved 5/5+3/3**. **Monday metrics:** packets closed 6–12 Jul = 16; session gates green 7/7 days (no nightly CI configured); D-burndown **4✅ (D1/D3/D4ˢ/D5) · 3◐ (D2/D6/D8) · 1☐ (D7)**; tick headline 30 Hz sustained over the 13-min live run (median dt 0.0333 s); evidence-pack build ~3 s on the 11k-row view (full-view Windows rebuild expected <15 s). **Next: Lemuel's ≤20-min finish list in `PENDING_EDITOR_GATES.md` (13-Jul banner) — film → closeout.**
- 2026-07-12 · WP_20260712 · **T+1 slip management — demo date passed, baseline re-proved, plan re-framed** (verification + docs only; **NO C++/wire/DTO/schema/product change, NO rebuild**). Checked, not assumed: the 11 Jul live session did NOT run (newest PIE run is still `unreal-test-run_20260708_195058`; film dir 0 stills / 0 clips; Step-0 KI-034 Live Coding recompile still pending). Re-ran the TC-50 baseline gate `python/verify_20260711.py` on 12 Jul: **PASS 5/5 + 3/3** (Z0 16/16 · link audit · preflight GO/DEMO READY · run-sheet current · pytest 10/10) → `wp_20260712_result.json`. First invocation FAILED G5 solely because the fresh sandbox lacked `pytest` (environment, not repo; installed → 10/10 → PASS; no KI — the gate correctly refused an unrunnable suite). Added a dated SLIP NOTE banner to `PENDING_EDITOR_GATES.md` (G4 markers intact, G4 re-PASSed after edit). **Decision: no new code lands until the ONE live session happens** — the 11 Jul run card is unchanged and valid; only its date slipped. D-gates UNCHANGED. **(same session, Lemuel-directed) full doc-staleness SWEEP:** refreshed banners/forward-looking text in 14 docs — Ops Manual (+`verify_20260711.py` documented), QA Plan (TC-50 = recurring baseline gate), KI Register review note, Troubleshooting NEW §H (KI-029/030/034/036 + sandbox-pytest symptoms), Maintenance (sandbox pytest note), Component Guide banner (8-Jul sensor confirmations + KI-034 pending), Analytical Review + Master Plan 12-Jul status notes, Launch Strategy + Traction Calendar GTM slip banners (re-baseline = Friday session), Sensor-Suite Roadmap + Hydrostatics-Plan clauses, WP_20260711 run-card slip pointer. Historical ledger/log/plan bodies untouched. TC-50 re-PASSed 5/5+3/3 after the sweep. **(same day, LIVE SCREEN SESSION pt 1):** Lemuel + Claude drove the pending in-engine queue via screen control. Step 0 found ALREADY CLEARED (DLL 9 Jul 05:59 postdates the KI-034 source fix 05:20 — no source newer than the build). FIRST LIVE RUN with the KI-034 DLL: `demo-monaco_capture_20260712_125800` (~13 min, 23,393 rows, 30 Hz) — TC-17 6/6, TC-23 6/6, TC-43 4/4 (AIS 3 mmsi / 1308 radar blips / camera frames), evidence pack + HTML built ⇒ G_AIS/G_RADAR/G_CAMERA re-confirmed on TODAY's build headlessly; G_TRAFFIC_UE orientation + G_HYDRO/G_PROP eye-checks pending Lemuel confirmation. **KI-037 found live + RESOLVED** (cp1252 UnicodeEncodeError in evidence-pack markdown writers; 4 files pinned UTF-8). Session friction logged: Claude's captures render only desktop+taskbar on this machine (all app windows hidden), `open_application` spawned stray editor instances, `python` PATH nondeterminism (3.13 has yaml, 3.14 doesn't) ⇒ DEMO_*.bat launchers pinned to the workspace `.venv`; port-probe bat added. Film/stills/SS5/COLREGS still to run this session. **(same session) screen-drive prep:** Lemuel directed running the pending in-engine checks via screen control — authored 7 double-click `DEMO_N_*.bat` launchers at the workspace root (run_demo monaco_capture / rough_turning_circle SS5, run_colregs ×4, verify_demo_session closeout; CRLF, pause-on-exit) so the session can be driven without terminal typing (computer-use terminals are click-only). Documented in Ops Manual §run commands. · gates 1/1 headless (+ live G2 = `verify_demo_session.py`, Lemuel) · evidence `wp_20260712_result.json` · authored via shell (KI-004).
- 2026-07-11 · WP-20260711 · **DEMO DAY (T-0): final GO gate + session run card** (verification + docs only; **NO C++/wire/DTO/schema/product change, NO rebuild** — nothing new lands on demo day by design). No 10-Jul session ran; no new PIE runs or media since 8 Jul (run 195058). Fresh same-day re-proof of the whole baseline: **Z0 16/16 · stacked link audit `verify_20260702b` 5/5+3/3 · `preflight_demo --report-only` GO (rehearsal DEMO READY) · pytest 10/10** — all rolled into NEW `python/verify_20260711.py` (**TC-50**): G1 Z0-json 16/16, G2 link audit, G3 preflight verdict GO, G4 run-sheet currency (PENDING_EDITOR_GATES.md still carries the Step-0 KI-034 re-open + Steps 5/6 markers — the KI-032 anti-drift check, now gated), G5 pytest; N1–N3 fixture controls (tampered sheet / NO-GO verdict / 15-of-16 Z0 all rejected). **PASS 5/5 + 3/3** → `wp_20260711_result.json`. `WP_20260711/PACKET.md` = the condensed demo-day run card (Steps 0→1→3→5→6, ~45–60 min, detail stays in PENDING_EDITOR_GATES.md). Session note: the known `io.open(newline=)` footgun truncated the new verify to 0 B mid-authoring — caught immediately by the line-count check, rebuilt in full + py_compile-verified (KI-004 protocol held). D-gates UNCHANGED — everything left (D2 SS5 eye-check, G_TRAFFIC_UE post-KI-034, D6 stills, D7 film, G_COLREGS_UE live matrix) closes only in Lemuel's ONE live session TODAY, ending with `verify_demo_session.py`. · gates 5/5 + neg 3/3 · evidence `wp_20260711_result.json` · authored via shell (KI-004), py_compile-verified.
- 2026-07-09 · WP-20260709D · **ONE-command demo-session closeout (T-2, fourth packet today)** (pure Python + docs; **NO C++**): NEW `python/verify_demo_session.py` runs the three existing gated tools (`verify_sensor_suite --latest`, `verify_capture_artifacts --latest [--film-dir]`, `verify_colregs --matrix`) as subprocesses and rolls them into `demo_session_result.json` with `gates_closed`/`gates_failed` (G_AIS/G_RADAR/G_CAMERA + G_CAPTURE_UE + G_FILM_UE + G_COLREGS_UE) plus the two remaining EYE-CHECKS listed honestly (G_TRAFFIC_UE orientation post-KI-034, D2 SS5 wave-ride); exit 0 iff every non-skipped section passes — a sub-tool FAIL is never masked (G3 proves it). Gated `verify_20260709d.py` **4/4 + 3/3 PASS** (`wp_20260709d_result.json`). Also FIXED stale `PENDING_EDITOR_GATES.md`: the G_TRAFFIC_UE row still named the removed fictional ships (SLOWBELLE/AZURFERRY/MERIDIAN, KI-035) — now the real Outliner labels — and Steps 5 (COLREGS live matrix via `run_colregs.py`) + 6 (this closeout) folded into the ONE-session plan. No D-gate status change (tooling only); the live session now ends with ONE command.
- 2026-07-09 · WP-20260709C · **Terminal COLREGS runner + KI-036 editor-spawn fix (T-2, Lemuel-directed, third packet today)** (pure-Python + editor-Python; **NO C++/wire/DTO/schema change, NO new rebuild**). Lemuel hit: Execute-Python-Script opened a NEW blank editor window — root cause **KI-036**: in editor Python `sys.executable` IS `UnrealEditor.exe`, so the picker's best-effort listener auto-launch spawned another editor. Fix per his directive (+3 answered decisions: scenery mode / auto-verify ON / keep picker as setup utility): new **`run_colregs.py`** — `--head-on|--crossing-giveway|--crossing-standon|--overtaking` chooses the scenario in code, rescue-boat default, foreground listener (`--once -v`, mmg via scenario), **auto-verifies THAT run dir** on PIE stop (per-scenario result file); `--ship` per-run swap, `--selftest` isolated 25× in `logs/_selftest`, `--dry-run`/`--list`/`--no-verify`. Picker refactored: auto-launch REMOVED, `__main__` = one-time `setup_scenery_target()` (assign rescue boat as TrafficActors[0], hide nothing, save level), `pick()`/`reset_all()` kept print-only. Also hardened `verify_20260709b` G4 (temp out-dir — was clobbering the canonical head_on result file; caught by today's gate). `verify_20260709c.py` **4/4+3/3** → `wp_20260709c_result.json`; KI-036 opened+RESOLVED; TC-48. In-engine remainder unchanged: one-time scenery setup + 4 terminal runs + `verify_colregs --matrix`.
- 2026-07-09 · WP-20260709B · **Rescue-boat default target + independently verifiable COLREGS (T-2, Lemuel-directed)** (Python + editor-Python + docs; **NO C++/wire/DTO/schema change, NO new rebuild** — the one pending recompile stays the KI-034 fix). Directive: all four COLREGS scenarios default to `marine_rescue_boat` (the only ship needing no import-roll correction), each independently run + independently verified, ship swappable per run. ONE source of truth: picker `TARGET_SHIP` (or `pick(enc, ship=…)`) → `--target-name` (new listener flag) → `make_field(target_name=…)` rename (+ `SHIP_TYPES`) → wire/evidence → manifest `aisTargetName` → verify. New **`python/verify_colregs.py`**: one command (`--latest`) reads the run's own manifest and gates health + verdict + target identity (cross-checks `sensor_raw.jsonl` wire names on PIE runs), writing a PER-SCENARIO result file; `--matrix` rolls up all four. Picker refactored (encounter→scenario map only; always passes the actual actor label) + new `reset_all()` restores the 3-ship scene for monaco_capture. Headless proof TODAY: **all four scenarios ran fresh (MMG) and verify COMPLIANT independently — matrix PASS 4/4**; swap e2e proven (`--target-name Yacht_with_interior` → manifest → verify PASS). `verify_20260709b.py` **5/5+3/3**; `verify_20260709` revised to the single-default model (re-PASS); `verify_20260629b` untouched-green. TC-47. In-engine remainder: run the 4 encounters in PIE (picker) → `verify_colregs --matrix` = G_COLREGS_UE evidence.
- 2026-07-09 · (same session as WP-20260709) · **Bridge Dashboard COMPLETE build guide (Word)** — Lemuel is mid-build on `WBP_BridgeDashboard` (layout + 1/19 bindings done) and the recipe doc lacked click-level steps. Authored `Documents/NaviSense_BridgeDashboard_Complete_Guide.docx` (10 sections, validated): status checklist, cached-pawn pattern, the exact Bind→Create-Binding template (his `Get_Rudder_Text` generalized), the FULL 19-binding reference table (widget name / function / getter / To-Text converter / format string), 3 control sliders + the per-tick re-issue chain gated on `IsDashboardControlActive`, viewport + B-toggle + **mouse-cursor/input-mode setup (gap in the recipe)**, stale-amber color binding, honesty labels, G_DASHBOARD_UE test script + 9-row troubleshooting table + full C++ surface appendix. Docs-only — no code, no gate change; G_DASHBOARD_UE (Step 2) remains Lemuel's in-editor task.
- 2026-07-09 · WP-20260709 · **Single-ship COLREGS with the REAL vessels — roll fix + names + plant (T-2, interactive)** · Lemuel's 8-Jul PIE run (`unreal-test-run_20260708_195058`, monaco_capture, 238 s, health 6/6) delivered THREE in-engine confirms: **`verify_sensor_suite --latest` PASS 4/4 on the real UE wire ⇒ G_AIS_SENSOR_UE + G_RADAR_UE + G_CAMERA_UE CONFIRMED (D4 in-engine sensor half CLOSED)**; G_TRAFFIC_UE ◐ (ships MOVE, orientation defect). Three defects fixed: **(1) KI-034** — `ApplyTraffic` hard-set `FRotator(0,yaw,0)` every packet, stomping the user's 90° mesh-import roll correction (the black "fin"); now captures each traffic actor's PLACED pitch/roll once and drives YAW only — **needs ONE recompile (Live Coding OK; Step 0 re-opened in the run sheet)**, Z0 16/16 + link audit 5/5 re-verified; **(2) KI-035** — rendered AIS presets renamed from fictional MERIDIAN/AZURFERRY/SLOWBELLE/PORTSTAR to the EXACT Outliner labels per rendered slot (picker mapping + monaco tag-scan order: excursion_vessel / marine_rescue_boat / Yacht_with_interior), naming rule documented, `verify_20260629b` updated + re-PASS 5/5+3/3; **(3)** the 28-Jun stub footgun for COLREGS — `Scenario` gains `plant`; the four `colregs_*` carry `plant="mmg"` and the listener resolves it when `--plant` is default. Why his run showed no COLREGS: monaco_capture is the transit/beauty scenario (stub, no maneuver) — the single-ship path is the four `colregs_*` scenarios via the WP-20260703 picker (recipe refreshed with the 9-Jul quick-start). `verify_20260709.py` **5/5+3/3** incl. e2e headless `colregs_head_on` **COMPLIANT** with the renamed target (+29° starboard, 238 m miss) → `wp_20260709_result.json`. TC-46 added; docs per protocol. Lemuel: recompile once, then run the 4 encounters (~15 min).
- 2026-07-08 · WP-20260708E · **Sensor-fidelity D4 yaw-rate false-FAIL fix (KI-033, T-3)** (test-tooling only; **NO C++/wire/DTO/schema/product-behavior change, NO rebuild** — Z0 untouched). Fifth session today; Lemuel away from the laptop (no screen-control, no live PIE possible), asked what else is genuinely headless to prep. Re-ran every verify tool that hadn't run today: `pytest` **10/10** (first run today), and a **fresh full** `demo_rehearsal.py --no-plot` (all 4 scenarios re-executed, not just aggregated) — 3/4 completed with fresh evidence packs, the 4th (`colregs_head_on`) was genuinely interrupted by this session's own tool budget mid-evidence-pack; re-running `preflight_demo.py --report-only` afterward proved the **KI-030 shadow-fix holds on a REAL interruption**, not just the synthetic fixture — it correctly fell back to the morning's complete run, verdict stayed **GO**. That regression pass then surfaced a real bug: `verify_sensors_fidelity.py`'s **D4 (yaw-rate) gate had no "not enough signal" guard**, unlike its sibling D5 (heading), which already reports INFO when a run barely turns. Run against a straight-transit log (`unreal-test-run_20260708_052109`, controller=transit — the same family as `monaco_capture`, `head_on_transit`, `harbor_traffic`) the plant yaw rate never leaves 0.000 deg/s, so `_pearson`/`_linfit` hit their zero-variance branches (corr=0.0, slope=0.0) and D4 hard-FAILed a run that simply never turned — a false FAIL, not a sensor defect. **Fix:** new `YAWRATE_MIN_RANGE_DEGPS=0.5` threshold; D4 now reports INFO below it, mirroring D5 exactly; PASS/FAIL math above the threshold is byte-identical to before — confirmed unchanged on the real turning-circle run `unreal-test-run_20260622_054815` (still corr=1.0000, still 8/8). Also added the `frozen_yawrate` negative control D4 never had (D5's `frozen_heading` pattern). New `python/verify_20260708e.py` **5/5 gates + 3/3 neg-controls** (G1 flat run now INFO+PASS not FAIL; G2 turning run unchanged PASS corr=1.0000; G3 new control fires FAIL on a turning run; G4 new control reports N/A on a flat run; G5 regression Z0 16/16 + preflight GO + py_compile; N1 replays the exact pre-fix math on the real flat run and proves it really did return 0.0/0.0 — the bug was real; N2 a synthetic run just above the threshold still gates to PASS — the guard doesn't over-suppress; N3 a flat plant with a corrupted sensor is still INFO — the guard reads the plant, not the sensor) → `wp_20260708e_result.json`. New **TC-45**; new **KI-033** opened+RESOLVED same session. D-gates unmoved — this protects any future run of the sensor-fidelity gate on a steady-course scenario from a false alarm, nothing else. **Session-status note added to `PENDING_EDITOR_GATES.md`:** Steps 1/3/4 queued unchanged awaiting Lemuel's live PIE session; Step 2 (Bridge Dashboard) deferred to Lemuel driving it directly (hand-built UMG graph wiring, not safe to automate blind). Backlog checked and correctly left untouched: WP-4 deterministic sim clock (KI-012) and live CesiumGeoreference (KI-014) are both in-engine C++/editor work — starting either now would force another rebuild or editor session 3 days before the demo for zero gate benefit; 6-DOF schema (KI-008) is effectively closed (rendered-surface sampling landed via hydrostatics 28 Jun); LiDAR/Sonar remain explicitly out of scope per the Sensor Suite Roadmap directive. · gates 5/5 + neg 3/3 · evidence `wp_20260708e_result.json` · authored via shell (KI-004), py_compile-verified.

- 2026-07-08 · WP-20260708D · **Preflight stale rebuild-guidance fix (KI-032, T-3)** (test-tooling text fix only; **NO C++/wire/DTO/schema/product-behavior change, NO rebuild** — Z0 untouched). Fourth session today, run after WP-20260708/B/C. Re-running the morning's regression to confirm nothing drifted surfaced a real problem in the regression tool itself: `preflight_demo.py`'s GO-branch "next" line hardcoded "proceed with PENDING_EDITOR_GATES.md Step 0 (full C++ rebuild, editor CLOSED) -> Steps 1-3" — accurate on 5 Jul (WP-20260705) but **wrong since 7 Jul**, when Step 0 was cleared and WP-20260708B re-scoped the run sheet to Steps 1-4. Left alone, the next demo-week `--report-only` GO would have told Lemuel to redo a rebuild he already completed — burning the one remaining PIE/capture slot and re-opening rebuild risk with 3 days to go. **Fix:** the GO-branch line no longer hardcodes a step count; it points at `PENDING_EDITOR_GATES.md` as the single source of truth and says to check its header before rebuilding again — evergreen, so it can't drift again next time a step clears. NO-GO branch untouched. New `python/verify_20260708d.py` **4/4 + 3/3 neg-controls** (G1 old string gone · G2 new text present in live stdout · G3 py_compile clean · G4 regression Z0 16/16 + preflight still GO; N1 a stale-text fixture is caught, N2 a missing-reference fixture is caught, N3 the NO-GO branch text is byte-identical) → `wp_20260708d_result.json`. Edited via shell (assert-guarded `str.replace`, KI-004 D: guard), 156 lines / py_compile clean / parens balanced 79-79. New **TC-44**; new **KI-032** opened+RESOLVED same session. D-gates unmoved — this protects the demo-week run sheet from a self-inflicted redundant rebuild, nothing else. Remaining work is unchanged: Lemuel's one PIE/capture session, Steps 1-4 of `PENDING_EDITOR_GATES.md`, ~40-50 min. · gates 4/4 + neg 3/3 · evidence `wp_20260708d_result.json` · authored via shell (KI-004), py_compile/AST-verified.

- 2026-07-08 · WP-20260708C · **sensor.v1 raw evidence sink + objective sensor-suite gate (T-3)** (pure-Python, additive; **NO C++/wire/DTO/schema/product-behavior change, NO rebuild** — Z0 untouched). Gap closed: the Step-1 in-engine sensor gates (G_AIS_SENSOR_UE / G_RADAR_UE / G_CAMERA_UE) were console eye-checks with NO on-disk evidence — `sensor.csv` flattens the wire and DROPS `ais.targets[]` / `radar.contacts[]` / `camera{}`. Now (1) `run_logger` persists a sampled raw copy of every `sensor.v1` packet to `sensor_raw.jsonl` (all of the first 300, then 1-in-10 ≈ 6 Hz @ 60 fps; envelope adds `wall_time`+`t_mono`, wire packet byte-intact under `msg`; best-effort/exception-swallowed so it can never break the bridge loop; manifest gains `sensorRawLines`), and (2) new `python/verify_sensor_suite.py` turns the three eye-checks into an exit-0/1 gate over a real run: R1 AIS 9-key targets + ranges TRACK, R2 radar blips ANONYMOUS (mmsi/name leak = FAIL) + all ≤ maxRangeM, R3 camera 7-key block + monotonic frameIndex + `.png` frameRef; `--latest` skips `_`-dirs, `--require` tailors non-traffic runs. `verify_20260708c.py` **4/4 + 3/3 neg-controls** (fixtures built with the SAME ais/radar/camera mirrors the C++ is parity-verified against; G4 regression: preflight rc0 **GO**, wp_20260708b intact) → `wp_20260708c_result.json`. Run sheet Step 1 now ends with the ONE objective confirm command; only G_TRAFFIC_UE stays visual. Lemuel cost: +1 command inside the already-planned PIE session. TC-43 added; KI-031 opened+RESOLVED (the evidence gap). D-gates unmoved on paper — this de-risks HOW D4's in-engine half gets closed at T-3.
- 2026-07-07 · **REBUILD CONFIRMED — Step 0 CLEARED (the single demo-critical action, T-4)** · Lemuel ran a full Build (editor CLOSED): **`Build: 53 succeeded, 0 failed`**, `[13/15] Link UnrealEditor-NaviSense.lib` + **`[14/15] Link UnrealEditor-NaviSense.dll`** (base DLL RELINKED — a real rebuild, not a Live Coding patch; the KI-018 lesson) + `[15/15] WriteMetadata`, `Result: Succeeded` (~56s). This one rebuild compiled the ENTIRE stacked 28 Jun→2 Jul additive C++ — dashboard 19 getters + SetHelm/SetThrottle/SetBowThruster (WP-20260701), `sensor.v1` ais/camera/radar blocks (WP-20260701B/C, WP-20260702), `traffic[]` render path (WP-20260629B), UMG/Slate/SlateCore module enable — validating the `verify_20260702b` pre-rebuild link audit (all 16 UFUNCTION decls defined ⇒ 0 link failures, as predicted). **Unblocks the entire in-engine queue** in `PENDING_EDITOR_GATES.md`: G_TRAFFIC_UE / G_AIS_SENSOR_UE / G_RADAR_UE / G_CAMERA_UE (Step 1 monaco_capture PIE), G_DASHBOARD_UE (Step 2), G_CAPTURE_UE + D2 SS5 wave-ride (Step 3) are now RUNNABLE. D-gate burndown UNCHANGED until the in-engine eye-checks report (the rebuild makes them runnable; it does not itself flip D2/D4/D6/D7). · tester=Lemuel (VS build log). · no code change this entry — milestone record.
- 2026-07-07 · KI-029 fix (interactive, T-4) · **Demo-readiness preflight flaked NO-GO — root-caused + hardened** (test-tooling only; **NO wire/DTO/schema/C++ change, NO rebuild — Z0 stays 16/16**). Lemuel hit `preflight_demo.py` → **NO-GO** with rebuild-safety green; traced to a wall-clock-starved `run_demo --selftest` TRUNCATING under load (turning-circle logged 4,251 rows vs ~10,300 ⇒ the turn never reached 180° ⇒ IMO `tactical_diameter`/`advance` = `None` ⇒ NOT READY; failing scenario jumped run-to-run = classic KI-012 non-determinism, not a build defect — rebuild-on-the-failed-run + a fresh direct run both PASS with DT **158.18 m**). Fix: `demo_rehearsal.rehearse()` now RETRIES a scenario (default 2, `--retries`) when the result looks truncated (`_looks_truncated`), but NOT on a deterministic content failure (health FAIL / wrong conformance). Proven: forced attempt-1 truncation → READY in 2 attempts; health-FAIL → 1 attempt (no mask). Post-fix `preflight_demo.py` → **GO** (rehearsal DEMO READY 2/2), `verify_20260704` 5/5+3/3 unchanged. Opened + RESOLVED **KI-029**; durable root fix (sim-step-driven self-test, retire KI-012) deferred post-demo. D-gate burndown unchanged. · authored via shell (KI-004), AST-verified.
- 2026-07-08 · WP-20260708 · **Demo-preflight false-NOT-READY fix (KI-030)** (test-tooling only; **NO C++/wire/DTO/schema/product-behavior change, NO rebuild** — Z0 stays 16/16). Today's `preflight_demo.py --report-only` returned **NO-GO** — a regression from WP-20260707. Root-caused **not** to a broken pipeline but to the demo-readiness *report-only* aggregator `demo_rehearsal._latest_run`: it picked each scenario's **newest run by mtime** then checked `evidence_pack/kpis.json`, so an **interrupted** run (killed mid-pack, no kpis.json) **shadowed** a good earlier complete run and flipped the whole preflight to a **false NOT READY** (seen live: `building_sea_transit` 054549 shadowed complete 054314; `colregs_head_on` likewise). `Z0` 16/16 + stacked link audit `verify_20260702b` 5/5+3/3 were green throughout (tree always rebuild-safe). **Fix:** `_latest_run` now returns the **newest run with a COMPLETE evidence pack**, falling back to newest+`None` when none is complete so an honest NOT READY (and a genuinely broken pipeline) is never masked; added `_read_kpis()`. New `python/verify_20260708.py` proves it on an isolated tmp fixture + re-derives the disk verdict: **PASS 4/4 + 3/3** (G1 older-complete beats newer-incomplete · G2 honesty fallback · G3 preflight==GO · G4 regression `verify_20260702b`+`verify_20260704` rc 0; N1–N3 neg-controls). Post-fix `preflight_demo.py --report-only` → **GO** (DEMO READY 4/4). **KI-030** opened+RESOLVED same session; **TC-41** added. D-gate burndown unchanged (no in-engine session ran) — D2/D4/D6/D7/D8 still gated on the ONE rebuild+PIE/capture slot, now **T-3**. · gates 4/4 + neg 3/3 · evidence `wp_20260708_result.json` · authored via shell (KI-004), AST-verified.
- 2026-07-08 · WP-20260708B · **D7 film gate + in-engine run-sheet refresh (T-3)** (capture tooling + docs only; **NO C++/wire/DTO/schema/product-behavior change, NO rebuild** — Z0 untouched). Two demo-week gaps closed: **(1) stale run sheet** — `PENDING_EDITOR_GATES.md` (2 Jul) and the morning packet still ordered the Step-0 full rebuild, but that rebuild was **confirmed done 7 Jul** (Build 53/0, DLL relinked); the sheet is refreshed (WP-20260708B): Step 0 marked **CLEARED — do NOT rebuild again** (no C++ changed since), session re-scoped to Steps 1–4 so Lemuel's one PIE/capture slot isn't spent on a redundant rebuild. **(2) D7 had no path/gate** — the 11 Jul soft-launch film (GTM: kit AT RISK) now has a pragmatic capture path (new **Step 4**: screen-record the Step-1/3 PIE runs via Game Bar Win+Alt+R / OBS, 60–90 s; honesty: a screen recording, NOT an MRQ cinematic — MRQ stays post-demo polish) and an **objective gate**: `verify_capture_artifacts.py` gains an additive optional **C3 FILM** check (`--film-dir` ⇒ ≥1 clip ≥5 MB, structurally valid — MP4/MOV `ftyp`+`mvhd` duration parse stdlib-only, MKV EBML, AVI RIFF — and ≥20 s for MP4/MOV; `--since-epoch` honoured so a stale grab can't pass; C1/C2 unchanged without the flag) = **G_FILM_UE**. New `python/verify_20260708b.py` **PASS 4/4 + 3/3** (G1 valid 30 s fixture accepted · G2 back-compat C1/C2-only without `--film-dir` · G3 run-sheet markers · G4 live `preflight_demo.py --report-only` rc 0 + **GO** + morning KI-030 gate intact; N1 undersized / N2 5-s / N3 no-ftyp `.mp4` rejected). **TC-42** added. D-gate burndown unchanged (no in-engine session ran) — D2/D4/D6/**D7** now all clear behind the ONE PIE/capture session (Steps 1–4), **T-3**. · gates 4/4 + neg 3/3 · evidence `wp_20260708b_result.json` · authored via shell (KI-004; session note: a bad `io.open(newline=)` arg in a patch script truncated `verify_capture_artifacts.py` to 0 B — caught by the line-count check, rebuilt in full + compile/CLI-verified same session).
- 2026-07-07 · WP-20260707 · **Demo-week hold-the-line (T-4)** (verification + docs only; **NO C++/wire/DTO/schema/Python-behavior change, NO rebuild** — Z0 stays 16/16). Nothing on disk changed since WP-20260706 (no new report/run/screenshot/code edit) ⇒ the ONE critical-path session (shared C++ rebuild + PIE/capture) is still un-run and every remaining demo gate (D2 SS5, D4 AIS/camera/GPS, D6 stills, D7 film, D8 clean-box) stays behind it (`PENDING_EDITOR_GATES.md`); the last six packets each flagged that piling on more headless code this close is the drift risk. Re-confirmed **GO** on today's disk (`preflight_demo.py`: Z0 16/16 · `verify_20260702b` 5/5+3/3 · `demo_rehearsal` DEMO READY, fresh 07 Jul run health PASS + COLREGS COMPLIANT) + regression `verify_20260704` 5/5+3/3; new `python/verify_20260707.py` re-derives the GO+regression verdict → `wp_20260707_result.json` (**PASS 2/2**). D-gate burndown unchanged (no in-engine session ran). Single critical-path action = Lemuel's Step-0 full `Build.bat` rebuild → Steps 1-3 PIE/capture, now **T-4**. · gates 2/2 auto + 0 new mandatory manual · evidence `wp_20260707_result.json` · authored via shell (KI-004), AST-verified.
- 2026-07-06 · WP-20260706 · **Demo-week hold-the-line (T-5) + Monday Week-5 metrics** (verification + docs only; **NO C++/wire/DTO/schema/Python-behavior change, NO rebuild** — Z0 stays 16/16). Deliberately not a new feature: the headless frontier is exhausted and every remaining demo gate (D2 SS5, D4 AIS/camera, D6 stills, D7 film) is behind the ONE shared rebuild + PIE/capture session (`PENDING_EDITOR_GATES.md`); the last five packets each flagged that piling on more headless code this close is the drift risk. Re-confirmed **GO** on today's disk (`preflight_demo.py`: Z0 16/16 · `verify_20260702b` 5/5+3/3 · `demo_rehearsal` DEMO READY, fresh 06 Jul health 7/7) + regression `verify_20260704` 5/5+3/3; new `python/verify_20260706.py` re-derives the GO+regression verdict → `wp_20260706_result.json` (PASS 2/2). D-gate burndown unchanged (no in-engine session ran). Single critical-path action = Lemuel's Step-0 rebuild → Steps 1-3 capture, now T-5.
- 2026-07-05 · WP-20260705 · **Demo GO/NO-GO preflight — one command re-confirms rebuild-safety + storyline-READY before the demo-critical PIE session** *(run on **Opus 4.8**, autonomous daily session — Sunday)* (pure-Python; **NO wire/DTO/schema/C++ change, NO rebuild — Z0 stays 16/16**; composes only already-verified tools, re-derives nothing ⇒ cannot drift). Context: 6 days to the 11 Jul demo, the headless frontier is exhausted, and the **entire** remaining critical path is ONE C++ rebuild + ONE PIE session (`PENDING_EDITOR_GATES.md`). Adding another headless feature this close is the flagged last-week *drift* risk; the higher-value move is to de-risk that single in-engine slot so Lemuel doesn't burn it on a tree that won't compile or has quietly drifted. New **`preflight_demo.py`** (workspace root) runs, on today's disk, (A) **rebuild-safety** — `Z0` compile-readiness (16/16) + `verify_20260702b` stacked link audit (all 16 pawn UFUNCTION decls defined, Build.cs UMG/Slate/SlateCore deps intact) — and (B) **storyline-READY** — `demo_rehearsal.py` → `demo_readiness.json overall_ready` — then aggregates to a single **GO / NO-GO** (`decide()` pure fn: GO iff every required check passed; empty/any-fail ⇒ NO-GO with the failing name) and writes `Saved/NaviSense_Reports/demo_preflight_result.json` (exit 0=GO / 1=NO-GO). Default re-runs the 2-scenario rehearsal fresh (can't false-green off a stale json); `--report-only` fastest, `--full` 4-scenario. **Deliverable run today: GO** — rebuild:Z0 rc0 (16/16) · rebuild:stacked_link_audit rc0 (5/5+3/3) · storyline:demo_rehearsal rc0 → **DEMO READY** (fresh 05 Jul run). New `python/verify_20260705.py` **5/5 + 3/3**: G1 `decide()` GO/NO-GO logic; **G2 e2e** live `--report-only` → GO + result-json keys/3-checks; G3 NO-GO on any required fail (no false green); G4 aggregation edges (empty⇒NO-GO, non-required fail doesn't block); G5 regression `Z0` 16/16 + `verify_20260702b` + `verify_20260704` rc0 + **3/3 controls** (N1 one failing required check⇒NO-GO, N2 empty⇒NO-GO, N3 a GO can never carry a failing required check). Also re-confirmed the tree is unchanged/green on today's disk: **Z0 16/16**, **verify_20260702b 5/5+3/3**, **demo_rehearsal --fast DEMO READY 2/2** (health 7/7, COLREGS COMPLIANT). Advances **D6/D8 tooling** (a repeatable pre-session go/no-go gate); **does NOT flip any D-gate status** — the in-engine `G_*_UE` eye-checks in `PENDING_EDITOR_GATES.md` remain. Honesty (KI-019 family): GO = the HEADLESS tree is rebuild-safe + the storyline is green — it does **not** confirm the in-engine demo; the tool's own banner + result-json `note` say so. New **TC-40**; no new defect found. · gates 5/5 auto + 3/3 controls + 0 new mandatory manual · evidence `wp_20260705_result.json` + `demo_preflight_result.json` · authored via shell (KI-004), AST/py_compile-verified.

- 2026-07-04 · WP-20260704 · **Headless demo-readiness rehearsal harness — one objective DEMO READY / NOT READY verdict across the demo storyline** *(run on **Opus 4.8**, autonomous daily session)* (pure-Python orchestration over the existing verified tools; **NO wire/DTO/schema/C++ change, NO rebuild — Z0 stays 16/16**; re-derives nothing itself so it cannot drift from the tools it aggregates). Context: WP-20260703 passed 5/5+3/3 and the headless frontier is exhausted — **every remaining demo gate is in-engine behind the one shared rebuild** (7 days to the 11 Jul demo). The last-week risk is now *drift*: a change that quietly breaks one of the scenarios the live demo shows. `run_demo.py` runs ONE scenario; nothing ran the whole storyline and reduced it to a single readiness verdict. New **`demo_rehearsal.py`** (workspace root) drives the curated **DEMO STORYLINE** headless — `imo_turning_circle` (D1/D6) · `imo_zigzag10` (D1/D6) · `building_sea_transit` (D2/D3 runtime sea-state sweep) · `colregs_head_on` (D4) — each via `run_demo.py --selftest` (no Unreal/GPU), reads each run's evidence pack (`health.verdict`, the IMO KPI pass flags, the COLREGS conformance verdict) and aggregates to ONE **DEMO READY / NOT READY** verdict + a per-scenario/per-gate readiness matrix (`--fast` 2-scenario smoke; **`--report-only`** re-aggregates the latest existing runs without re-running, for a nightly re-report). Deliverable run today (full storyline, `--plant mmg`): **DEMO READY — 4/4 required scenarios READY, gates D1/D2/D3/D4/D6 covered headless** (`logs/_rehearsal/DEMO_READINESS.md` + `Saved/NaviSense_Reports/demo_readiness.json`). New `python/verify_20260704.py` **5/5 + 3/3**: G1 storyline↔registry parity (4 required, all real, IMO keys well-formed), G2 evaluator correctness (a good pack READY, a health-FAIL/no-pack NOT ready), **G3 e2e** — the `--fast` rehearsal runs 2 real `--selftest` runs ⇒ DEMO READY 2/2, D1+D4 covered, reports written, G4 aggregation (`overall_ready`: one required-fail ⇒ NOT READY, a non-required fail does not block, empty ⇒ NOT READY), G5 regression (`Z0` 16/16 + `verify_20260703`/`_20260702b` green) + **3/3 controls** (N1 an unknown scenario detected not-run, N2 a health-FAIL pack scored NOT ready — no false green, N3 a conformance mismatch scored NOT ready). Advances **D6** (demo tooling — a repeatable readiness gate) + seeds **D8** (headless repro of the whole storyline); **does NOT flip any D-gate status** (the four in-scenario gates were already green headless — this proves they stay green together). Honesty (KI-019 family): READY = the HEADLESS pipeline is green; it does NOT confirm the in-engine demo (the G_*_UE eye-checks in `PENDING_EDITOR_GATES.md` remain), and the report banner says so. New **TC-39**; no new defect found. · gates 5/5 auto + 3/3 controls + 0 new mandatory manual · evidence `wp_20260704_result.json` + `demo_readiness.json` · authored via shell (KI-004), py_compile-verified.
- 2026-07-03 · WP-20260703 · **Single-target COLREGS encounters + scripted own-ship avoidance** · *(Opus 4.8, Lemuel-directed reconstruction)* (pure-Python + editor-Python + docs; **NO C++/wire/DTO/schema change, no rebuild — Z0 stays 16/16**). Replaces the confusing 3-ships-at-once `monaco_capture` with a clean pick-one-encounter setup: ONE target ship runs a chosen COLREGS scenario relative to own-ship, own-ship runs its MMG model, compliance scored. **New `ColregsAvoidController`** (`scenario_controllers.py`) — a deterministic, closed-loop **heading hold** on the plant's authoritative yaw (invariant #5): give-way encounters make an early bounded **starboard** alteration (ramped setpoint, PD-damped) held through CPA then resume; the stand-on encounter **holds** course/speed (Rule 17). **Four scenarios** `colregs_head_on` (R14) / `colregs_crossing_giveway` (R15, target from starboard) / `colregs_crossing_standon` (R17, target from port) / `colregs_overtaking` (R13), each on a dedicated single-target `*_avoid` AIS preset kept SEPARATE from the transit presets (so the existing scenarios + `verify_20260624`/`_20260627` are unchanged — confirmed green). **New `--initial-speed` flag + `Scenario.initial_speed_mps`** give the stand-on a running start so Rule-17 'keep speed' is measured on a steady vessel, not a 0→cruise spin-up. **In-editor picker** `Phase5_Systems/10_colregs_encounter.py` hides the two unused Traffic ships, assigns the chosen ship as `TrafficActors[0]`, launches the listener (+ 4-button EUW recipe `Documents/NaviSense_COLREGS_Encounter_Recipe.md`). `verify_20260703.py` **5/5 + 3/3**: G1 registry, G2 controller profiles, **G3 e2e** — all four scored the intended verdict on real `--selftest --plant mmg` runs (**head_on COMPLIANT 238 m**, **crossing give-way COMPLIANT 342 m passed astern**, **overtaking COMPLIANT 295 m**, **stand-on COMPLIANT held course+speed, 152 m near-miss**), G4 picker, G5 Z0 16/16 + `verify_20260702b`/`_701b`/`_629b` green; controls N1 held-course head-on→NON_COMPLIANT, N2 port wrong-way crossing→flagged, N3 stand-on early turn→NON_COMPLIANT. Advances the **D4 / §5.1 W5-6 COLREGS differentiator** (the conformance metric is now selectable + demonstrably scores a COMPLIANT give-way, not just a held-course NON_COMPLIANT). In-engine **G_COLREGS_UE** (one ship moves, own-ship maneuvers, verdict shown) — no rebuild needed. New **TC-38**, **KI-028** (honesty: scripted maneuver vs a scripted target = the METRIC, not autonomous avoidance — W5-6). · gates 5/5 auto + 3/3 controls + 1 in-engine (G_COLREGS_UE) · evidence `wp_20260703_result.json` · authored via shell (KI-004), py_compile-verified.

- 2026-07-02 · WP-20260702B · **Pre-rebuild integration audit + consolidated one-session in-engine runbook** · *(run on **Opus 4.8**, autonomous daily session)* (HEADLESS audit + docs; **NO new C++, NO wire/DTO/schema change, NO new rebuild risk**). Context: with the three headless D4 sensor items + the Radar roadmap item shipped by 1–2 Jul, the headless frontier is essentially exhausted — **every** remaining demo gate is in-engine behind **one shared C++ rebuild**, now the single most demo-critical action (11 Jul, 9 days out). That rebuild carries a week of stacked additive C++ incl. a **rebuild-FORCING `Build.cs` module change** (`UMG`/`Slate`/`SlateCore`); nothing was auditing the combined surface headless and `PENDING_EDITOR_GATES.md` was 2 weeks stale. New `python/verify_20260702b.py` audits the whole stack: **5/5 gates** (G1 `Z0` **16/16**; G2 Build.cs module deps present & un-commented AND `Json`/`JsonUtilities`/`Sockets`/`Networking` survived; **G3 the link-failure guard Z0 lacks** — all **16** new pawn UFUNCTION decls have `.cpp` definitions + the retained-traffic accessor; G4 the `ais`/`camera`(`bEmitCamera`)/`radar`(`bEmitRadar`) `sensor.v1` blocks wired, pull from `GetTrafficTargets()`, both SBC TUs brace-balanced; G5 regression — `verify_20260701`/`_701b`/`_701c`/`_702`/`_629b` all exit 0) + **3/3 negative controls fire** (N1 a commented-out `UMG` dep → G2 fails; N2 a declared-but-undefined UFUNCTION → G3 fails; N3 the radar block removed → G4 fails) ⇒ the stacked surface is expected to **compile + link first try**. Rewrote `Development/work_packets/PENDING_EDITOR_GATES.md` into a single ≤20-min session: one full rebuild (editor CLOSED, NOT Live Coding — module deps changed) → `--scenario monaco_capture` PIE clears **G_TRAFFIC_UE/G_AIS_SENSOR_UE/G_RADAR_UE/G_CAMERA_UE** → dashboard (`09_build_bridge_dashboard.py` + recipe) **G_DASHBOARD_UE** → capture (`08_capture_demo_stills.py` + `rough_turning_circle` SS5) **G_CAPTURE_UE** + the remaining **D2** wave-ride re-check. **Does NOT move the D1–D8 burndown** (headless audit; gates stay where they are — it de-risks the action that closes them). New **TC-37**. · gates 5/5 auto + 3/3 controls + 0 new mandatory manual · evidence `wp_20260702b_result.json` · authored via shell (KI-004), py_compile-verified.

- 2026-07-01 · WP-20260701C · **Camera sensor (WP-14, still-frame metadata) on `sensor.v1` — closes the D4 camera item** · *(run on **Opus 4.8**, autonomous daily session)* (additive C++ wire block + reusable Python mirror + verify; **NO DTO/USTRUCT/schema-struct, NO controller change** — `sensor.v1` is raw JSON so the B1 wire-parity guard + `Z0` stay 16/16; rides the SAME pending rebuild as WP-20260701/701B, **no separate rebuild**). **Decision:** of the two remaining concrete D4 closers — #3 live-Cesium GPS and #4 Camera (WP-14) — I closed **#4 first** because it is the **lower-risk** path: Cesium routes through `CesiumRuntime`, the direct cause of the OPEN S2 **KI-014** editor crash (the in-engine gate carries real crash risk against the 11-Jul demo), whereas the camera still-frame approach **reuses the WP-20260630 `HighResShot` pipeline already in the tree** (no new engine plugin, no crash surface). **Chosen approach: still-frame (metadata) camera, NOT a live `SceneCaptureComponent2D` pixel feed** — streaming pixels over the socket is heavy + out of demo scope; instead the wire carries camera **capture metadata** + a deterministic `frameRef` that names the HighResShot still on disk. `USensorBundleComponent::BuildSensorsJson()` now emits a `sensor.v1 camera{}` block (gated on `bEmitCamera`): `{fovDeg,resX,resY,headingDeg,frameIndex,frameRef,pose{x,y,z}}` — pose = own-ship chase-rig position in the wire frame (`Wire.X/Y/Z` = East/Up/North m), heading = the IMU block's `HeadingDeg`, `frameRef = Printf("%s%05d.png", CameraFramePrefix, CameraFrameIndex++)` (monotonic per-call). Header gains 5 camera UPROPERTYs (`bEmitCamera=true`, `CameraFovDeg=90`, `CameraResX=3840`, `CameraResY=2160`, `CameraFramePrefix="NaviSense_"`) + a private `CameraFrameIndex`. New reusable **`python/camera_sensor.py`** (stdlib-only mirror — `camera_record()`/`frame_ref()` with the same keys/frameRef naming/defaults + an honesty note, an INDEPENDENT reimplementation so a future dashboard/consumer can reuse it + the verify can prove the C++). New `verify_20260701c.py` **5/5** (G1 the C++ block is wired — `bEmitCamera` gate, `camera`/`frameRef`/`pose` fields, monotonic `CameraFrameIndex++`, chase-rig pose + `HeadingDeg`, header fields present; **G2 parity** — the mirror's `frameRef` matches the C++ `Printf("%s%05d.png")` naming (`NaviSense_00000.png`…) + maps pose (East,Up,North)+index exactly; G3 schema/honesty — right keys+types, wire-frame pose, FOV/res/prefix defaults match C++↔mirror, the **"NOT a live … feed"** honesty label is present in both, frame index monotonic; G4 determinism; **G5 regression** — `Z0` **16/16** + `verify_20260701b`/`verify_20260701`/`verify_20260629b` all still PASS) + **3/3 controls fire** (N1 an unwired/`bEmitCamera=false` stub detected not-wired; N2 a wrong `frameRef` — missing zero-pad / wrong prefix — caught; N3 the metadata tracks pose/heading + the index advances). Advances **D4** (the last concrete headless-buildable sensor item — camera frames — is now on the wire; the pixels come from the existing HighResShot capture). Remaining concrete D4 = **live `CesiumGeoreference` GPS** (deferred behind KI-014 de-risking). New **TC-35**; new **KI-026** (honesty: metadata + on-disk stills, not a live in-band video feed); **KI-009** updated. · gates 5/5 auto + 3/3 controls + 1 in-engine (**G_CAMERA_UE**, needs the rebuild) · evidence `wp_20260701c_result.json` · authored via shell (KI-004), Z0/py_compile-verified.

- 2026-07-01 · WP-20260701B · **AIS → `sensor.v1` feed — own-ship AIS receiver (closes a concrete D4 gap)** · *(run on **Opus 4.8** at Lemuel's request after the 1-Jul Sonnet scheduled run — see [[feedback_model_consistency]])* (additive C++ + reusable Python mirror + verify; **NO new DTO/USTRUCT — `sensor.v1` is built as raw JSON, so the B1 wire-parity guard + `Z0` stay 16/16**; folds into the pending WP-20260701 dashboard rebuild, **no separate rebuild**). The gap: `USensorBundleComponent::BuildSensorsJson()`'s `ais.targets[]` was **hardcoded empty** — the own-ship never reported the scripted contacts it renders (WP-15B). Fix: the pawn now **retains** the last applied contact list (`TArray<FNaviSenseTrafficTarget> LastTraffic` set atop `ApplyTraffic`) + exposes `GetTrafficTargets()`; the SBC emits one `ais.targets` record per contact `{mmsi, name, rangeM, trueBearingDeg, relBearingDeg, cogDeg, sogKn, latDeg, lonDeg}` — identity/course/speed from the contact + **receiver-computed** range & true/relative bearing from own-ship (wire `x=East, z=North`; compass 0=N/90=E; rel +ve=starboard), lat/lon via the **same geo origin the GPS block uses** (43.7350/7.4250). Empty list ⇒ `[]` (byte-identical back-compat). New reusable **`python/ais_sensor.py`** (stdlib-only receiver-feed builder, an INDEPENDENT reimplementation so a future sensor.v1 AIS fuser/dashboard can reuse it + the verify can prove the C++ math). New `verify_20260701b.py` **5/5** (G1 the C++ is wired — pawn retains `LastTraffic`/`GetTrafficTargets`, SBC emits FROM it with all 9 keys, not the empty array; **G2 geometry parity** — the mirror matches the canonical `ais_traffic.range_bearing`/`relative_bearing` across **48 contact-instants** (2 presets × 3 own headings × 4 times) to **0.00e+00**; G3 schema/back-compat/identity — right keys+types, `mmsi==contact id`, geo-origin lat/lon, **no traffic ⇒ `[]`**; G4 determinism; **G5 regression** — `Z0` **16/16**, dashboard `verify_20260701` **5/5** + traffic `verify_20260629b` **5/5** both still PASS) + **3/3 controls fire** (N1 the old hardcoded-empty block detected as not-wired; N2 a swapped wire→receiver frame caught 87°; N3 the feed tracks identity/course — id/cog changes reflected, not a stub). Advances **D4** (the AIS-on-`sensor.v1` half — GPS/IMU validated, AIS analysed, now the receiver actually reports contacts); remaining concrete D4 = live `CesiumGeoreference` GPS + Camera (WP-14). New **TC-34**; **KI-009** updated. Honesty (KI-019 family): the own-ship AIS **receiver view of scripted, deterministic contacts** — not a live receiver / not autonomy; `rangeM`/bearing are geometry, not the CPA/TCPA/COLREGS verdict; lat/lon is the flat-earth projection (Cesium is the separate item). · gates 5/5 auto + 3/3 controls + 1 in-engine (**G_AIS_SENSOR_UE**, needs the rebuild) · evidence `wp_20260701b_result.json` · authored via shell (KI-004), Z0/py_compile-verified.

- 2026-07-01 · WP-20260701 · **Interactive Bridge Dashboard — data + control layer (WP-UI-DASHBOARD, directed by Lemuel 30 Jun)** (additive C++ + editor-Python + docs; **NO DTO/wire/schema change** — `FNaviSenseState` keeps its 22 wire keys, Z0 stays 16/16; `SensorBundleComponent.cpp` untouched). Lemuel's locked scope: full-screen, navy-themed, **interactive** dashboard — actuators / sensors / maneuver+IMO KPIs / sea-state+AIS — with helm/throttle/bow-thruster controls that drive the ship. This packet ships the headless-authorable half: `NaviSense.Build.cs` **enables `UMG`/`Slate`/`SlateCore`** (Phase 10 HUD, previously commented — **needs a C++ rebuild**); `NaviSenseCoords.h` gains a geo-projection helper (`METERS_PER_DEG_LAT` + `NorthMToLatDeg`/`EastMToLonDeg`) mirroring `SensorBundleComponent`'s existing inline GPS projection so the dashboard's live lat/lon agrees with the sensor.v1 GPS block; `ANaviSenseShipPawn` gains **19 new `BlueprintPure` getters** across the four panels (rudder/RPM/thruster; heading/SOG/yaw-rate/roll/pitch/heave/lat/lon; motion-mode/plant-mode/rolling-advance/peak-heading-deviation; traffic-contact-count/nearest-range/nearest-name) plus **3 `BlueprintCallable` control entry points** `SetHelm`/`SetThrottle`/`SetBowThruster` (each clamps to `[-1,1]`) that route into the EXISTING manual-drive path (`UpdateManual` now sources its throttle/rudder targets from the dashboard commands once active, plus a new bow-thruster yaw term at zero way — real thrusters work regardless of way, unlike the rudder). New `python/verify_20260701.py` **5/5** (G1 all 19 getters present; G2 the 3 control entry points clamp + mark dashboard-active; G3 `UpdateManual` demonstrably mirrors the dashboard commands incl. the bow-thruster→yaw and →`bowThrusterNorm` paths; G4 `UMG`/`Slate`/`SlateCore` active in Build.cs; **G5 e2e** `Z0` **16/16**, navy-theme palette byte-parity between the new editor helper script and the recipe doc, geo-projection constant parity vs `SensorBundleComponent`) + **3/3 negative controls fire** (N1 a commented-out UMG dep caught; N2 an un-clamped control body caught; N3 a removed getter caught AND named). New editor-Python `Phase5_Systems/09_build_bridge_dashboard.py` (prints the navy theme + full getter/setter binding list; best-effort creates the `WBP_BridgeDashboard` widget-blueprint stub — UMG graph wiring itself is not reliably Python-scriptable on this engine build) + new `Documents/NaviSense_BridgeDashboard_Recipe.md` (full manual build/binding/control-wiring recipe, mirrors the WP-16 wake recipe pattern). Honesty (KI-019 family): the live maneuver/IMO panel is a rolling kinematic PROXY, not the post-run CFD-validated evidence-pack KPI; AIS range is geometry only, not the logged CPA/TCPA/COLREGS verdict; both the recipe doc and the widget itself must label these. This is an additional Lemuel-directed deliverable, not one of the D1–D8 demo gates — it does not move the burndown below. · gates 5/5 auto + 3/3 controls + 1 in-engine (**G_DASHBOARD_UE**, needs a C++ rebuild) · evidence `wp_20260701_result.json` · authored via shell (KI-004), Z0/AST-verified.


- 2026-07-02 · WP-20260702 · **Radar sensor — Sensor Suite Roadmap Pt 1 (NEW scope beyond D4)** (additive C++ wire block + reusable Python mirror + verify + §5 design record; **NO DTO/USTRUCT/schema change, NO controller change, no recompile of its own** — rides the shared pending rebuild). The 1-Jul directive has two tiers: the last concrete **D4** item (live `CesiumGeoreference` GPS) is blocked behind the OPEN S2 **KI-014** Cesium editor-crash (in-engine risk, not safely closeable headless), and the NEW Radar/LiDAR/Sonar suite (§4.5-7) each needs the §5 design decisions recorded **before** any C++. With the three headless D4 items shipped 1 Jul, the highest-value headless move is roadmap item #5 — **Radar** — the natural extension of the validated AIS receiver geometry (a contact list, no ray traces). **NEW** `Documents/NaviSense_Sensor_Suite_Roadmap.md` records the §5 decisions for all three (Radar realised; LiDAR/Sonar design-recorded incl. the Sonar seabed-mesh prerequisite) + the scope flag + KI-019 honesty discipline. **C++:** `SensorBundleComponent.{h,cpp}` emit `sensor.v1 radar{maxRangeM,sweepDeg,contacts[]}` gated on `bEmitRadar` — each contact within `RadarMaxRangeM` (12 NM) is an **anonymous blip** `{rangeM,trueBearingDeg,relBearingDeg,radialSpeedKn,closing}` (NO identity) from `Pawn->GetTrafficTargets()`; range/bearing reuse the AIS geometry, radial (range-rate) speed from own (speed+heading) + target (cog/sog) velocity, `closing=radial<0`; beyond-range dropped; no traffic ⇒ `[]`. **No new USTRUCT** (raw JSON) ⇒ B1 + `Z0` stay **16/16**. **NEW** `python/radar_sensor.py` reusable mirror; **NEW** `python/verify_20260702.py`. `verify_20260702` **5/5 + 3/3**: G1 C++ wired, **G2 geometry parity 0.00e+00** vs `ais_traffic` across 48 instants + radial-sign check, G3 schema/anonymity(no mmsi/name)/beyond-range-drop/default-match/KI-027 honesty, G4 determinism, G5 regression (`Z0` 16/16 + `verify_20260701b`/`_701c`/`_629b` all PASS + roadmap doc exists); controls N1 unwired-detected, N2 range-drop + closing-sign, N3 tracks-geometry. Opened **KI-027** (honesty: geometric radar derived from scripted contacts, NOT an EM/RCS simulation — no clutter/false alarms). New **TC-35**. **Does NOT move the D4 gate** (see the Sensor Suite Roadmap line below); in-engine **G_RADAR_UE** (blips on the wire during PIE) pending the shared rebuild. · gates 5/5 auto + 3/3 controls + 1 in-engine (G_RADAR_UE) · evidence `wp_20260702_result.json` · C++/Python authored via shell (KI-004), Z0/AST-verified.

- 2026-06-30 · WP-20260630 · **Demo-capture enablement — D6 stills / D7 frames + the in-engine batch** (pure-Python + editor-Python; **NO wire/DTO/schema/C++ change, NO recompile, no new mandatory in-engine gate of its own**; fully additive, zero existing files edited). The headless frontier is now exhausted (29 Jun): every remaining demo gate — **G_TRAFFIC_UE** (29-Jun traffic render, pending the rebuild), **D2** SS5 wave-ride re-check, **D6** ≥3 beauty screenshots, **D7** film — is in-engine and needs Lemuel + PIE. This packet makes capture turnkey + gateable. **NEW** `python/verify_capture_artifacts.py` (stdlib-only D6/D7 gate — **C1** ≥3 PNG stills, each ≥ min bytes AND ≥ min W×H read straight from the PNG **IHDR** so a blank/failed HighResShot or a low-res grab is rejected, `--since-epoch` scopes it to one session; **C2** the named run passes `verify_run_kinematics` so a beauty frame is never accepted over a spinning/NaN run; writes `capture_artifacts_result.json`, exit 0/1). **NEW** editor-Python `Phase5_Systems/08_capture_demo_stills.py` (burst `HighResShot 3840x2160` on the editor tick — non-blocking, re-frame between shots — + writes `capture_manifest.json` with the session epoch + the exact verify command). New `verify_20260630.py` **5/5** (G1 the capturer parses + really drives HighResShot + the manifest; G2 the shots check accepts a full-res set; G3 the health check is wired to `verify_run_kinematics` + PASSES on a real run; G4 the stdlib PNG-IHDR parser reads size + rejects non-PNG bytes; **G5 e2e** `evaluate()`=PASS for {3×4K stills + a healthy run}, the four capture scenarios resolve to real controllers, **Z0 16/16** C++ untouched) + **3/3 controls fire** (N1 only 2 stills → shots FAIL, N2 a 640×480 grab → shots FAIL, **N3 a spin-on-the-spot run → run-health FAIL** so the gate won't rubber-stamp a broken run). Also **batches the four pending in-engine gates into ONE ≤20-min session** (`--scenario monaco_capture` G_TRAFFIC_UE + D7 COLREGS beat → `rough_turning_circle` D2 SS5 wave-ride → `building_sea_transit`/`storm_ride` D7 reel → `verify_capture_artifacts --latest`). Regression on current disk green: **Z0 16/16** (C++ untouched), `verify_run_kinematics` **PASS** on the newest real run, CLI smoke (shots-only full-res set PASS; `--latest` pre-capture correctly C1-FAIL / C2-PASS). Advances **D6/D7 tooling** — the in-engine **G_CAPTURE_UE** (`verify_capture_artifacts --latest` PASS on a real captured set) remains. New **TC-32**. Capture-readiness note (NOT a defect, no new KI): the 29-Jun `yacht_with_traffic.png` shows neon-cyan water + "No Loaded Region(s)" — re-open `NaviSense_Monaco` so the WaterZone reloads the 28-Jun deep-blue material + regions before framing stills. Honesty: ships **tooling, not footage** — it does not itself flip a gate to ✅. · gates 5/5 auto + 3/3 controls + 1 in-engine (G_CAPTURE_UE) · evidence `wp_20260630_result.json` · authored via shell (KI-004), py_compile/AST-verified.

- 2026-06-29 · WP-20260629B · **Real Traffic ships for COLREGS scenarios (WP-15B)** — wire-driven in-engine rendering of scripted AIS contacts (Lemuel added 3 vessels under *Traffic*; **needs a C++ rebuild**). Decisions: Python drives over the wire · custom 3-ship encounter · static-mesh props. **C++:** new `FNaviSenseTrafficTarget` (own header so B1 stays 22/22) + `FNaviSenseState.traffic[]` (optional/additive, no schema bump) + pawn `ApplyTraffic()` (same `NaviSenseCoords` + own-ship spawn anchor, keeps placed Z, forces Movable, tag auto-resolve) + bridge forwards it (RX thread still UObject-free, invariant #2). **Python:** `ais_traffic.wire_targets()` + new **`monaco_capture`** 3-ship preset (overtaking SLOWBELLE + crossing AZURFERRY + head-on MERIDIAN; own-ship give-way to all) + listener builds the field once at run start and emits per-tick `traffic[]` + `scenarios.py` entry. **Editor:** `Phase5_Systems/07_setup_traffic_ships.py` (Movable+tag+assign+save). `verify_20260629b.py` **5/5 + 3/3**: **G2 capture-correctness** — replicating the C++ anchor, each rendered ship lands EXACTLY ahead_m/starboard_m of own-ship for an arbitrary spawn pose (**0.000 cm**, spawn-invariant) ⇒ what's captured == what's scored; **G5 e2e** — the real listener with `--scenario monaco_capture` emits **55 packets × 3 moving targets**; + G1 wire/back-compat, G3 COLREGS-valid (overtaking/crossing/head_on, give-way), G4 determinism; controls N1 unknown-preset, N2 wrong-frame caught (223 446 cm), N3 render tracks course (flip COG ⇒ 720 m). Regression green: Z0 **16/16** (B1 22/22, invariants intact), `verify_20260624` **6/6+3/3**, `verify_20260627` **6/6+3/3**, `verify_20260629` **6/6+3/3**, `run_demo --selftest --scenario monaco_capture` **DEMO COMPLETE health 6/6** (pack carries 3 targets + COLREGS conformance). Advances **D4** (AIS contacts now RENDERED, not just analysed) + unblocks a **D7** COLREGS film. New **TC-31**; **KI-009** updated (WP-15B rendering half delivered headless; in-engine **G_TRAFFIC_UE** pending rebuild). Honesty: visual props on a deterministic preset — not a live AIS receiver / not autonomy. · gates 5/5 auto + 3/3 controls + 1 in-engine (G_TRAFFIC_UE, needs rebuild) · evidence `wp_20260629b_result.json` · C++/Python authored via shell (KI-004), Z0/py_compile-verified.

- 2026-06-29 · WP-20260629 · **Canonical run-clock `t_mono` — closes KI-024** (pure-Python, log-schema add; **NO wire/DTO/schema/C++ change, NO recompile, no new in-engine gate**; append-only). KI-024: `state.csv t` (Python plant clock) and `sensor.csv t` (UE engine clock) advance **3.0× vs 1.0× wall** under high PIE FPS, so any fuse-by-`t` of the two logs is badly misaligned; the 22-Jun mitigation only joined `verify_sensors_fidelity` on `wall_time` (a fragile epoch float, not guaranteed monotonic across NTP steps). Fix: the single `RunLogger` now stamps ONE monotonic, run-relative clock `t_mono = time.monotonic()-run_start` on **every row of BOTH logs** (+ `events.csv`) and a `manifest.timeBase` block; `t_mono` is now THE canonical join key — `verify_sensors_fidelity` prefers it (`_join_key`), falling back to `wall_time` for legacy (pre-`t_mono`) runs so every historical run still joins; `t` stays the raw per-side reference. New `verify_20260629.py` **6/6** (G1 `t_mono` in both logs + `manifest.joinKey`; G2 monotone + run-relative; **G3 one timeline while raw `t` diverges 3×** — sensor 5.90 s vs state 17.70 s; G4 fidelity prefers `t_mono` on a fresh run + falls back to `wall_time` on `…_055244`; G5 canonical join recovers the plant ramp **rms 0.0001 m/s**; G6 a `t_mono`-stripped run still joins) + **3/3 negative controls fire** (N1 backwards `t_mono` caught, N2 desynced/rescaled `t_mono` caught, **N3 the bug the fix prevents — a raw-`t` join is ~3000× worse, rms 0.580 vs 0.0001 m/s**). Regression on current disk green: Z0 **16/16** (C++ untouched), `verify_run_kinematics` **8/8** (`…_055244`), `verify_sensors_fidelity` **8/8** (`…_055244`, `wall_time` fallback identical, C1 still flags the 3.0×/1.0× divergence), `run_demo --selftest` **DEMO COMPLETE health 8/8 DT 158.2 m IMO PASS** (the produced run carries `t_mono`; fidelity confirms *"join uses 't_mono'"*), `verify_20260623` e2e **5/5 + 3/3** (27.0 s). Hardens **D4** (sensor fidelity) + **D6** (evidence-pack integrity — the DIANA artifact) by removing the fuse-by-`t` trap before any sensor-overlay / WP-15B AIS fuser is added. **KI-024 RESOLVED.** New **TC-30**. Honesty: this makes the join key robust + enforced; it does NOT make the two raw clocks identical (deterministic engine sim-clock = **KI-012**, in-engine). · gates 6/6 auto + 3/3 controls + 0 new manual · evidence `wp_20260629_result.json` · authored via shell (KI-004), py_compile-verified.

- 2026-06-28 · **D6 capture look — natural ocean colour fixed** · The UE Water plugin ocean read as neon cyan. Root-caused: the colour is the SingleLayerWater **Absorption/Scattering** on the water material **instance** (`MPC_Water` carries only wave/sim data — verified by `06_fix_water_mpc_color.py` dumping all 53 vector + scalar params, none a colour), and the **WaterZone caches materials at level load** so MI edits only show after a **reload**. New `Phase5_Systems/05_set_ocean_water_color.py` sets Absorption (8,70,420)/Scattering (0,0.25,0.40) on whatever MIs the WaterBody uses; re-opening NaviSense_Monaco rebuilt the sea to a natural deep-blue depth gradient (Lemuel-confirmed). New Troubleshooting entry. · in-engine; tester=Lemuel.
- 2026-06-28 · **D5 wake & spray CONFIRMED in-engine (G_WAKE_UE mechanic)** · Built `NS_Wake` (Niagara, Fountain base) + `WakeViz` component on `BP_ShipPawn_Yacht`; Event Tick drives `User.WakeIntensity`/`User.Spray` from the C++ `GetWakeIntensity01/Spray01` (rebuild confirmed — the getter nodes resolved in BP). Tuned to a speed-driven **white foam trail** (Local Space OFF=world; velocity 500-850→20-80, gravity -980→0, lifetime 1.4→4-6 s, spawn ∝ WakeIntensity). Wake thins at rest, fills with way ⇒ G_WAKE_UE mechanic met; Lemuel accepted 'done for now', final cinematic polish folds into D7. Recipe doc updated with exact module locations. · tester=Lemuel; C++ via the WP-20260628 rebuild, Niagara/BP in-engine.
- 2026-06-28 · **KI-021 manual drive RESOLVED in-engine** · Created `GM_NaviSense` (GameModeBase, **Default Pawn Class = None**) + set it as `NaviSense_Monaco` World Settings **GameMode Override** so the placed yacht (Auto Possess Player 0) is possessed instead of a default character — `M` toggles MANUAL, W/S/A/D drive, and the deck-level distorted view is fixed by the same change. Tester=Lemuel; no code change. Closes the WP-6 fallback gate. · (Live session continues on D5 wake look-polish + D6/D7.)
- 2026-06-28 · **D2 G_HYDRO float-at-waterline CONFIRMED in-engine + listener footgun caught** · Live session (Lemuel drives, Claude verifies). (1) **Turn-spin regression diagnosed = NOT a code regression:** a `turning_circle` was spinning on the spot (effective radius **3.6 m**, |r| 0.420 rad/s) because the run used the default **`--plant stub`** (kinematic) instead of `--plant mmg`; the stub plays back a fixed commanded yaw rate. Proven from the logs (28 Jun stub: round u=1.5/r=0.420, radius 3.6 m vs 24 Jun mmg: u=1.279893/r=0.018811, radius ~83 m). Re-run with `--plant mmg` ⇒ natural ~83 m circle restored. Pawn turn logic (RInterpTo + spawn-anchor translation) intact; no code touched. New Troubleshooting entry. (2) **D2 float:** with hydrostatics active (`DA_DOLPHIN_HydrostaticsConfig`) the hull rode ~one draft too high (props/thruster above water); fix = `Hydrostatics → WaterlineOffsetCm = -218` cm (≈ DOLPHIN draft 2.177 m; `Z = sampled surface + offset`), tuned live + **saved on the instance** — hull now settles at the boot-top, props+thruster submerged. **KI-022 RESOLVED.** Remaining D2 = SS5 wave-ride re-check with hydrostatics owning roll/pitch/heave. Follow-up: bake `WaterlineOffsetCm=-218` into the `BP_ShipPawn_Yacht` Hydrostatics default for clean-repro. · no code change (property tune + command fix); tester=Lemuel (screenshots).
- 2026-06-28 · WP-20260628 · **Speed-driven wake & spray VFX (D5 / WP-16) — first in-engine packet in a week; the headless frontier was exhausted** (D2/D5/D7 are in-engine; six prior packets were pure-Python). The hull is kinematically posed (no fluid sim), so the wake is **driven by speed**: a Niagara system reads two 0..1 user floats (`WakeIntensity`, `Spray`) and scales bow wave / stern wash / spray. **New** `python/wake_model.py` = the single source for the speed→VFX curve (intensity 0 below a 0.3 m/s dead-band, linear to 1 at ~20 kn; spray gated until ~15 kn hull speed; ribbon width / spawn / spray-rate), grounded in DOLPHIN's range, stdlib-only + deterministic. **Minimal additive C++** (no wire/DTO/schema change, no coordinate/sign work — invariant #1 intact): the three kinematics getters become `UFUNCTION(BlueprintPure)` (Niagara/HUD-readable) and the pawn gains `WakeFullSpeedMS=10.3 / WakeSprayOnsetMS=7.7 / WakeMinSpeedMS=0.3` UPROPERTYs + `GetWakeIntensity01()` / `GetWakeSpray01()` whose C++ bodies **mirror `wake_model.py` exactly**; Z0 **16/16**, both TUs brace-balanced. **New** `Phase5_Systems/04_setup_wake_vfx.py` (attaches a `WakeViz` NiagaraComponent at the stern, assigns `NS_Wake` if built, seeds the user floats — script not the crashing menu, KI-013) + `Documents/NaviSense_Wake_VFX_Recipe.md` (NS_Wake emitter build + live-intensity binding + a **no-recompile fallback** that drives the same curve from BP position-delta, so D5 can progress before the rebuild). `verify_20260628.py` **6/6** (G1 intensity 0-at-rest/monotone/clamped/=1-at-full; G2 spray gated below the onset then ramps; G3 ribbon/spawn/spray-rate in bounds + monotone; **G4 the parity gate** — the C++ defaults **==** the Python constants and each getter uses the right constant + clamps, so the rendered curve is the gated one; G5 deterministic; G6 editor-script parses + recipe references the user params + edited C++ brace-balanced) + **3/3 negative controls fire** (N1 a flat curve is rejected, N2 a wake-at-rest is rejected, N3 a tampered C++ constant is detected by parity). Regression on current disk green: Z0 **16/16**, `verify_run_kinematics` **8/8** (run `…_055244`), `verify_sensors_fidelity` **8/8** (morning run), `verify_20260623` run_demo e2e **5/5 + 3/3** (27.4 s). Advances **D5 ☐→◐** (feed + curve + recipe done; the NS_Wake art + **G_WAKE_UE** eye-check remain) and unblocks **D7** (the film needs a wake). New **TC-29**; new **KI-025** (honesty: the wake is a speed-driven visual proxy, not a fluid/CFD wake — do not claim a physical wake in GTM). · gates 6/6 auto + 3/3 controls + 1 in-engine (G_WAKE_UE) · evidence `wp_20260628_result.json` · C++ authored via shell (KI-004), Z0/py_compile-verified.

- 2026-06-27 · WP-20260627 · **COLREGS conformance scoring — the V&V differentiator (§5.1), delivered early/headless** (pure-Python analysis layer; **NO wire/DTO/schema/C++ change, NO recompile, no new mandatory in-engine gate**; one new module + two additive, guarded evidence-pack hooks). WP-20260624 put a scripted AIS target on the run and *classified* the encounter + CPA/TCPA; it never *scored* whether own-ship complied. New `python/colregs_score.py` is that missing layer: for each encounter it asks **did the logged own-ship maneuver conform to the COLREGS duty?** and returns a per-target verdict (COMPLIANT / NON_COMPLIANT / NOT_APPLICABLE) with rule checks + reasons. **Give-way** (head-on/crossing/overtaking) is COMPLIANT iff the maneuver was *substantial* (Rule 16/8b: peak alteration ≥15° or ≥25% speed cut), *early* (Rule 8a: action began at TCPA ≥90 s), in the *correct direction* (Rule 14/15: net to starboard for head-on/crossing), reached a *safe distance* (Rule 8d: closest pass ≥200 m) and did **not** cross ahead (Rule 15). **Stand-on** is COMPLIANT iff course+speed were *held* until close-quarters (Rule 17a-i); action only after close-quarters is permitted (17a-ii/b). No risk-bearing encounter ⇒ NOT_APPLICABLE (a clear pass is not a violation). Stdlib+`math` only, deterministic, mirrors `analyse_ais` so the encounter classification matches that module. `build_evidence_pack.py` attaches `ais['conformance']` (try/except — never breaks the pack) + a COLREGS-conformance sub-table in `EVIDENCE.md`; `evidence_html.py` renders green/red verdict pills + an honesty footer; `run_demo.py` ships it automatically for any `--ais`/traffic scenario. New `verify_20260627.py` **6/6** (G1 a prompt early starboard give-way scores COMPLIANT 4/4 checks; G2 a held course into a head-on scores NON_COMPLIANT — Rule 16 no action + Rule 8d unsafe; G3 a stand-on hold scores COMPLIANT while an early alter scores NON_COMPLIANT — Rule 17a-i; G4 the decision-moment encounter/duty **matches `analyse_ais`** on the real run `…_055244`; **G5** the evidence pack's verdict **equals** the standalone scorer (parity) and the IMO KPIs + health are **byte-identical** to a no-AIS build — purely additive; G6 determinism bit-identical) + **3/3 negative controls fire** (N1 a clear/opening pass is NOT_APPLICABLE, never a false violation; N2 a give-way turning the **wrong way (to port)** is flagged on the starboard check; N3 a tampered `kpis.json` verdict is **detected** by the parity comparison). Live e2e: `run_demo --scenario head_on_transit --selftest` → DEMO COMPLETE, health PASS; the auto pack honestly scores the held-course transit **NON-COMPLIANT** (give-way, no avoiding action). Regression on current disk green: `verify_run_kinematics` **8/8**, `verify_sensors_fidelity` **8/8**, `verify_20260623` run_demo e2e **5/5 + 3/3** (26.6 s), `verify_20260624` AIS **6/6 + 3/3**, `verify_20260626` HTML report **6/6 + 3/3** (exercised the edited pack/HTML path). Advances **D4** (the AIS/intelligence half — CPA/TCPA was data, this is the rule-conformance verdict) + **seeds the week-5-6 COLREGS-scoring differentiator** (§5.1): the metric now exists + is gated; the avoidance *controller* that makes own-ship PASS is next. D4 stays ◐. Honesty (KI-019 family): the scorer **measures** the logged maneuver — it does NOT make own-ship avoid traffic (autonomy = W5-6). The pack section + HTML footer say so; do not present a scripted-run verdict as autonomous COLREGS compliance. New **TC-28**. · gates 6/6 auto + 3/3 controls + 0 new mandatory manual · evidence `wp_20260627_result.json` · authored via shell (KI-004), py_compile/AST-verified.

- 2026-06-26 · WP-20260626 · **Single-file HTML evidence report (D6 / WP-19)** (pure-Python presentation layer; **NO wire/DTO/schema/C++ change, NO recompile, no new mandatory in-engine gate**; one existing file edited additively + one new module). The D6 evidence pack now emits a **self-contained `evidence_report.html`** next to `EVIDENCE.md`/`kpis.json`: every plot is embedded as a base64 `data:` URI (zero external file refs → the one .html can be emailed / dropped into a grant portal and still renders), with the IMO maneuvering-KPI table (PASS/FAIL vs limits), the kinematic-health verdict + all checks, the actuator-correspondence table, the AIS/COLREGS section, and an honest provenance footer (MMG standard-method KPIs **not** CFD-validated; roll/pitch/heave = visual proxy; AIS = scripted). New `python/evidence_html.py` (stdlib-only renderer — it *formats* the dicts `build_pack` already computed, never re-derives ⇒ no drift); `build_evidence_pack.py` calls it by default (`--no-html` to skip) and records `report_html` in `kpis.json`; the generator tag bumped to v2. `run_demo.py` now ships the HTML automatically (confirmed: `--selftest` turning-circle → 201 KB report, 2 plots embedded, 0 external refs). New `verify_20260626.py` **6/6** on the real run `unreal-test-run_20260624_055244` (G1 builds+parses; G2 all plots embedded as **valid** base64 PNGs == plot count; G3 IMO-KPI parity vs `kpis.json` — DT 158.18 m / DT-Lpp 4.16 / advance 155.31 m verbatim; G4 health verdict PASS + every check id present; G5 AIS/COLREGS section with MERIDIAN head_on + range/encounter; G6 self-contained + portable — copied alone to a temp dir, still renders) + **3/3 negative controls fire** (N1 a magic-tampered base64 image is DETECTED invalid, N2 a wrong KPI value is DETECTED by the parity check, N3 an injected external `<img src>` is FLAGGED). Regression on current disk green: `verify_run_kinematics` **8/8** (run `…_055244`), `verify_sensors_fidelity` **8/8** (morning run, C1 KI-024 divergence still flagged/joined on wall_time), `verify_20260623` run_demo e2e **5/5 + 3/3** (26.6 s — this exercised the new HTML path live), `verify_20260624` AIS **6/6 + 3/3**, `repro_doctor` **READY** + `run_demo --selftest` **DT 158.2 m IMO PASS**. Advances **D6** (the evidence pack is now a single shareable artifact — the thing handed to a pilot / DIANA reviewer); D6 stays ◐ (remaining = ≥3 in-PIE beauty screenshots / MRQ). Honesty (KI-019 family): the report formats verified numbers + labels what they are/aren't; no new claim. · gates 6/6 auto + 3/3 controls + 0 new mandatory manual · evidence `wp_20260626_result.json` · authored via shell (KI-004), AST-verified.

- 2026-06-25 · WP-20260625 · **Clean-machine reproducibility — readiness doctor + repro gate + documented setup (D8)** (pure-Python + docs; **NO wire/DTO/schema/C++ change, NO recompile, NO new mandatory in-engine gate** — zero existing files edited, fully additive). New `python/repro_doctor.py`: a **stdlib-only** (runs BEFORE `pip install`) readiness/reproducibility doctor — 12 checks, each OK/WARN/FAIL + an actionable fix + a `required` flag, `--strict` (demo-day) fails on a required WARN, writes `Saved/NaviSense_Reports/repro.json`. Checks python version, **requirements.txt deps importable**, **core Python tools present + parse-clean (KI-004 truncation guard)**, MMG plant, **.uproject parses (engine 5.7 / Water on / Cesium ref)**, **key C++ brace-balanced (KI-015 guard)**, **DA_DOLPHIN vessel+hydrostatics assets present AND not bare Git-LFS pointer stubs**, **Cesium ion token (optional, documented)**, git+git-lfs, logs writable, scenario registry (11), free port. New `SETUP.md` (workspace root): the documented `git clone`→demo path (git lfs pull, venv + `pip install -r requirements.txt`, `repro_doctor`, the **Cesium ion token** BYO + why headless doesn't need it = D8 "tokens documented", headless `run_demo --selftest`, the in-engine run, troubleshooting). `verify_20260625.py` **6/6** (G1 doctor READY on the real tree 10 OK/2 WARN/0 FAIL; G2 deps enumerated exactly — 3 active, optionals excluded, `pyyaml→yaml`, all import; G3 .uproject engine 5.7 + Water on + both data assets present & not LFS stubs; G4 no-token ⇒ `cesium_token`=WARN+optional, verdict still READY + SETUP.md documents the token; **G5 the core** — `run_demo --selftest` ×2 ⇒ both health PASS + same IMO verdict + DT within 2%, observed **158.18 m == 158.18 m IMO PASS**; G6 schema-valid `repro.json` + SETUP.md references) + **3/3 negative controls fire** (N1 a missing required dep ⇒ verdict NOT ready; N2 a required WARN is READY by default but NOT ready under `--strict`; N3 a 130-byte `version https://git-lfs` stub is flagged a pointer while a real binary passes — the "LFS not pulled" trap). Regression on current disk green: `verify_run_kinematics` **8/8** (newest real run `…_055244`), `verify_sensors_fidelity` **8/8** (morning run), `verify_20260623` run_demo e2e **5/5 + 3/3** (28.0 s). Advances **D8 ☐→◐** (documented setup + automated readiness verdict + headless repro proven; remaining = the one-time clean-box in-engine confirm, which also inherits D2/D5/D7). Honesty (KI-019 family): the doctor checks prerequisites + proves the HEADLESS pipeline reproduces; it does NOT claim the in-engine demo runs on a clean box (labelled manual gate G_REPRO_UE). · gates 6/6 auto + 3/3 controls + 0 new mandatory manual (optional G_REPRO_UE) · evidence `wp_20260625_result.json` · authored via shell (KI-004).

- 2026-06-24 · WP-20260624 · **Scripted AIS traffic + CPA/TCPA + COLREGS encounters (D4 / WP-15)** (pure-Python, read-only over the own-ship track; **NO wire/DTO/schema change, NO recompile, NO new mandatory in-engine gate** -- the listener's `--ais` only records the preset in the run manifest, zero hot-path work). New `python/ais_traffic.py` (deterministic AIS targets + `cpa_tcpa` analytic closed form + range/rel-bearing/aspect + `classify_encounter` for COLREGS Rules 13-15 -> give-way/stand-on + Monaco-origin lat/lon matching the GPS sensor + AIS Type-18 encoder + presets head_on/crossing/overtaking/harbor_mix) and `python/analyse_ais.py` (reconstructs each scripted target over a logged run's own-ship track -> per-target range/bearing/CPA/TCPA time series, min-CPA, **decision-moment** encounter (first CPA alert, else min range), CPA alerts; writes `ais.csv` + a range/CPA plot). `build_evidence_pack.py` gains `--ais`/manifest auto-read -> an **AIS & COLREGS** section + `ais.csv` + `ais_cpa.png` + a `kpis['ais']` block (IMO maneuver KPIs + health gate **untouched**). `scenarios.py` + a new `TransitController` (`--controller transit`, steady straight course = clean own-ship for an encounter) add 4 traffic scenarios (`head_on_transit`/`crossing_transit`/`overtaking_transit`/`harbor_traffic`) that compose with `run_demo.py`. `verify_20260624.py` **6/6** gates (G1 analytic cpa/tcpa/range/bearing; G2 COLREGS classify on a steady synthetic track -- head_on/crossing/overtaking all correct + own duty; **G3 the WP-15 gate** -- the AIS block lists the target with the correct range/bearing from own-ship = 1600.38 m / 1.25 deg vs hand geometry; G4 evidence integration on the real run `unreal-test-run_20260624_055244` with turning KPIs DT/Lpp 4.16 + health 8/8 **unchanged**; G5 determinism bit-identical; G6 manifest->pack auto-read chain) + **3/3 negative controls** (N1 unknown preset rejected -- `make_field` raises + listener exits !=0; N2 a receding target -> `no_risk`, never alerts; N3 a PORT-bow crossing -> `stand_on`, the opposite of the starboard give-way). Live end-to-end confirmed headless: `run_demo --scenario head_on_transit --selftest` -> **DEMO COMPLETE, health PASS**, head_on/give_way/alert (24,715 rows). Regression on current disk: `verify_run_kinematics` **8/8** (newest real run), `verify_sensors_fidelity` **8/8** (morning run), `verify_20260623` (run_demo e2e) **PASS** (5/5 + 3/3). Advances **D4** (AIS data/analysis half) + seeds **W5-6 COLREGS scoring** (CPA/TCPA + rule-conformance metrics). UE AIS-pawn rendering + `sensor.v1` mmsi/cog/sog = **WP-15B** (in-engine follow-up). Honesty (KI-019 family): targets are scripted/deterministic, labelled as such; no AIS *receiver* or *validated traffic model* claimed. · gates 6/6 auto + 3/3 controls + 0 new mandatory manual (optional G_AIS_UE) · evidence `wp_20260624_result.json` · authored via shell (KI-004).

- 2026-06-24 · **KI-023 twin propellers RESOLVED in-engine** · Lemuel split the DOLPHIN props in Rhino and re-exported them AT ORIGINAL POSITION at the hull's scale (centimeters / FBX UnitScaleFactor 1.0 -- a first mm/USF-0.1 export came in 10x oversize), so they import straight onto the shafts (meshes `Pp`/`Sp`). The split-mesh pivot sits ~3.6 m from the hub (mesh origin = Rhino world origin) => spinning the mesh DIRECTLY orbits; fixed WITHOUT re-export by parenting each mesh under an empty `Scene` pivot `prop_port`/`prop_starboard` placed at the hub with local **X along the shaft** -- the actuator-viz resolver (KI-023 code, WP-20260621_HYDRO) matches the PIVOT and the blades follow on-axis. Result: both props **rotate in place, counter-rotating**, `propeller=twin`. Also assigned `DA_DOLPHIN_VesselProfile` + `DA_DOLPHIN_HydrostaticsConfig` (advances the HYDRO in-engine path; **G_HYDRO float-at-waterline still to confirm**). Done on the LEVEL INSTANCE (placed pawn) -- works for the demo on save; **follow-up: recreate in the `BP_ShipPawn_Yacht` class** for permanence/clean-repro. Process lessons: match a part's export units to the hull (cm/USF 1.0); for a single-mesh part whose pivot can't sit at the hub, an empty named pivot + child mesh spins it correctly with no re-export. · in-engine confirm (Lemuel); no code/recompile.

- 2026-06-23 · WP-20260623 · **Scenario Runner v0 — one-command demo (D6 / seeds D8)** (pure-Python orchestration; **NO wire/schema/C++ change, NO recompile, no new mandatory in-engine gate**). New `run_demo.py` (workspace root) turns the demo into ONE command: PREFLIGHT (python/layout/scenario/free-port/tool-import — the seed of the **D8** clean-machine check) → LAUNCH the listener `--once` with a named scenario → DRIVE one run (interactive PIE **or** `--selftest`, a bundled `ue5_client_sim.py` vessel = no Unreal/GPU) → POST-RUN auto-build `build_evidence_pack.py` + the `verify_run_kinematics` health gate, printing a one-line `DEMO COMPLETE` summary; exit 0 iff preflight+run+health all pass. New `verify_20260623.py` gates it END-TO-END headless: **5/5** (G1 preflight, G2 list↔registry parity, G3 turning-circle run→`kpis.json`+`EVIDENCE.md`+plots+health PASS, G4 KPI parity vs standalone `build_evidence_pack`, G5 zig-zag other-controller overshoot) with **3/3 negative controls firing** (N1 invalid scenario→preflight FAIL, N2 busy port→FAIL, **N3 no-vessel→'no run logged', NO fake pass** — this control caught + fixed a stale-dir fallback in `_new_run_dir`). Headless `--selftest` rehearsals isolated in `logs/_selftest/` (+ `--log-dir` override) so they never shadow a real run for `--latest` tooling. Regression on current disk: `verify_run_kinematics` **7/7** + `verify_sensors_fidelity` **8/8** on the real morning run `unreal-test-run_20260622_054815`. Advances **D6** (one-command runner now exists, ◐) + seeds **D8**. · gates 5/5 auto + 3/3 controls + 0 new mandatory manual (optional G_DEMO_UE eye-check with real UE). · evidence `wp_20260623_result.json` · authored via shell (KI-004; confirmed the sandbox can't unlink on D: — only rename).

- 2026-06-22 · **HYDRO C++ compile FIX — rebuild Succeeds (KI-022 unblocked)** · Lemuel's first full Build-Solution rebuild of WP-20260621_HYDRO surfaced 2 real errors at `NaviSenseHydrostaticsComponent.cpp:52` (**C2664/C2451**): the 21-Jun switch to UE 5.7's `TryQueryWaterInfoClosestToWorldLocation` kept the old bool+out-param call shape, but the 5.7 function RETURNS a `TValueOrError<FWaterBodyQueryResult,EWaterBodyQueryError>` and takes an optional query-Z as arg 3. Fixed to capture the return + read via `HasValue()`/`GetValue()`; Z0 **16/16**, braces balanced. **Rebuild now GREEN — `Build: 1 succeeded, 0 failed`, `[1/4]` compiled NaviSenseHydrostaticsComponent.cpp + `[3/4]` relinked `UnrealEditor-NaviSense.dll` (base DLL, not a Live Coding patch), Result: Succeeded — Lemuel-confirmed.** Unblocks in-engine **G_HYDRO/G_PROP**: next = create/assign `DA_DOLPHIN_HydrostaticsConfig` (`06_create_hydrostatics_config.py`, KI-013) + tune `WaterlineOffsetCm`. · authored via shell (KI-004).

- 2026-06-22 · **WP-20260622: sensor-fidelity gate (D4/F2)** (pure-Python, read-only over `logs/`; **NO recompile, NO DTO/wire/schema change, NO new in-engine gate**). New `python/verify_sensors_fidelity.py` cross-validates the in-engine sensor bundle (`sensor.csv` GPS/IMU) against the authoritative plant pose (`state.csv`), turning the never-objectively-checked WP-SENSOR-1 S1–S3 eye-checks ("values non-zero & in range") into a real *accuracy* verdict: it derives the expected sensor from the plant exactly as `SensorBundleComponent` does, so a faithful sensor is a near-affine image (slope≈1, corr≈1) of the plant. 8 gates (D1 timing, D2 finite+fix, D3 speed, D4 yaw-rate, D5 heading, D6 position, D7 geo-projection, D8 accel) + 2 INFO (D9 AIS, C1 clock). **CRITICAL JOIN FINDING:** `state.csv` `t` (Python plant clock) and `sensor.csv` `t` (UE engine clock) diverge under high PIE FPS (**3.0× vs 1.0×** on the morning run) ⇒ the gate joins on `wall_time`, never on `t`, and flags the divergence as **KI-024** (S3). Verified on **two** real runs: Lemuel's morning `unreal-test-run_20260622_054815` (turning_circle SS0) **8/8** and the 21-Jun 493° turn `163148` **8/8** — speed/yaw-rate corr **1.0000** (RMS ≤0.001), heading corr **1.0000 over a 492° sweep**, GPS position = plant + spawn offset with **median residual 0.18 m / 0.43 m**, geo-origin **43.7350 N, 7.4250 E** consistent to ~3e-7°. Negative controls **5/5 fire** (constant speed→D3, frozen heading→D5, scrambled lat/lon→D7, NaN accel→D2, lost fix→D2). Regression on current disk: `verify_run_kinematics` on the morning run **7/7**, WP-SENSOR-1 S1–S3 PASS. Advances **D4 ☐→◐**. · evidence `wp_20260622_result.json` · authored via shell (KI-004; the editor/​shell desync truncated the first sandbox copy — rebuilt via heredoc, `py_compile` clean, 531 lines).

- 2026-06-21 · **WP-20260621_HYDRO: hydrostatics port (analytic) + twin-prop drive** · Ported Lemuel's Unity hydrostatics to UE as an analytic C++ integrator (decision: analytic / full port now). New `NaviSenseHydrostaticsConfig` (DOLPHIN data) + `NaviSenseHydrostaticsComponent` (samples the rendered WaterBody along hull strips → integrates buoyancy/GM damped heave/roll/pitch oscillators); Water module enabled in Build.cs; pawn now uses hydrostatics for heave/roll/pitch (Python keeps X/Y/yaw; wire proxy = fallback). This is the REAL fix for the floating (KI-022): the hull settles at the true waterline by buoyancy equilibrium and rides waves. Twin-prop drive ported (port/stbd RPM, counter-rotating; KI-023) with single-mesh fallback. `verify_hydrostatics.py` **9/9** (DOLPHIN periods heave 2.89/roll 6.63/pitch 2.72 s match analytic; equilibrium, damping, strip-slope, clamp, determinism); Z0 **16/16**; all files brace-balanced. **Pending: full rebuild + create/assign the config data asset + tune WaterlineOffsetCm + Blender prop split.** Math proven headless; UE Water-API call + kinematic integration are the in-engine unknowns. · authored via shell (KI-004).

- 2026-06-21 · **Deep investigation: Unity hydrostatics + twin-prop → UE port plan** · Lemuel reported the yacht floating above the waterline and twin props spinning around one point, and pointed to his Unity hydrostatics script/controller. Investigated `NaviSense Simulator/Assets/Scripts/` and found a portable hybrid seakeeping model: `Hydrostatics/{HydrostaticsConfig,HydrostaticsController,RigidbodyHydroSetup,*WaveSampler}` + `Ship/YachtPoseApplier` — Python owns X/Z/yaw, physics owns heave/roll/pitch via buoyancy + GM restoring + damping sampling the Crest ocean (DOLPHIN data: 366 t, GMt 1.044, GMl 68.154…). This is the REAL fix for the floating (buoyancy equilibrium at the true waterline), vs. the current UE visual proxy + hardcoded Z. Twin props: Unity used two counter-rotating pivots; UE imported the FBX as one merged mesh. Wrote `Documents/NaviSense_Hydrostatics_Port_Plan.md` (architecture + UE port options A1 Chaos-parity / A2 analytic integrator + propeller split). Water plugin already enabled in the .uproject (only the Build.cs module line is commented). Interim placement fix for floating authored (KI-022). Opened KI-023 (twin-prop). **Awaiting Lemuel's decisions (impl approach, timing vs 11 Jul demo, propeller source mesh) before building.**

- 2026-06-21 · **KI-020 spawn anchoring (run starts at the placed yacht, not the port)** · Lemuel found PIE runs snapped the yacht back inside Port Hercule (maneuvering through buildings) despite moving the actor to open water. Root cause: `ApplyOwnShipState` applied the wire pose as an ABSOLUTE world position (`WireToUE` has no translation ⇒ origin=port). Fix authored in `NaviSenseShipPawn`: capture placed transform at BeginPlay, apply wire pose as a spawn-heading-rotated OFFSET when `bAnchorPoseToSpawn` (new EditAnywhere bool, default on); signs still single-sourced in `NaviSenseCoords` (invariant #1). Z0 **16/16**. **CONFIRMED in-engine 21 Jun** (Lemuel: run starts at the placed open-water spot, no port teleport); KI-018 regression 8/8 on the same build. · authored via shell (KI-004).

- 2026-06-21 · **KI-018 RESOLVED in-engine + G7 sign + D1 closed** · Live in-engine verify session (Lemuel drives PIE, Claude verifies logs). First post-Live-Coding rerun STILL spun ⇒ diagnosed stale patch (LC doesn't rebuild the base DLL); a **full `Build.bat` rebuild** (relinked `UnrealEditor-NaviSense.dll`) + relaunch fixed it. Post-fix `turning_circle` run `unreal-test-run_20260621_163148`: **493° continuous turn, no spin**, `verify_run_kinematics` **8/8**, evidence pack **DT/Lpp 4.16 IMO PASS**. G7 (+rudder⇒starboard) confirmed. Closes **KI-018** and the **D1** sign-confirm. Also confirmed live: NaviSense_Monaco 19/19 actors, Cesium signed-in + Google 3D Tiles present (**KI-014 likely resolved**), FPS ~40. · evidence `wp_20260621_result.json` + `logs/unreal-test-run_20260621_163148/evidence_pack/`.

- 2026-06-21 · WP-20260621 · **Runtime sea-state schedule (D3) + named-scenario registry (D6)** (pure-Python; rides the existing `heaveM`/`rollDeg`/`pitchDeg` keys; **NO DTO/wire/schema change, NO recompile, NO new in-engine gate**). New `ScheduledSeaState`/`parse_schedule` in `python/sea_state.py` makes a single run sweep ≥3 sea states by CROSS-FADING one deterministic `WaveField` per state (continuous in t ⇒ no jitter, exact at each set-point, seeded once ⇒ replayable, composes with WP-4's clock); it duck-types `WaveField` so `build_state_packet`/`wave_response` sample it unchanged. New `python/scenarios.py` registry + listener `--scenario <name>` / `--sea-state-schedule "t:ss,…"` (explicit flags win; `--scenario list` prints the menu); each integer sea-state crossing is logged to `events.csv` and the schedule/scenario to `manifest.json`; `build_evidence_pack.py` now surfaces both. `verify_20260621` **10/10** with negative controls (a malformed schedule is rejected; a naive hard-switch trips the continuity bound the cross-fade clears — 0.218 m vs 0.0005 m; an easing schedule's energy goes DOWN), incl. an END-TO-END headless listener run logging **6** sea states and a scheduled `turning_circle` evidence pack with IMO KPIs. Regression on current disk green: WP-20260620 **8/8**, Z0 **16/16**, schema-v13 **12/12**, WP-9 **9/9**, re-accept **5/5**, pytest **10/10**. Advances **D3** (runtime switch — the last ☐ half; logging landed in WP-9) + **D6** (scenario selection). · gates 10/10 auto + 0 new manual (folds into the pending Session-A recompile). · authored via shell (KI-004), py_compile/AST-verified.

- 2026-06-20 · WP-20260620 · **Run-log health gate + D6 IMO-KPI evidence pack** (pure-Python, read-only over `logs/`; **NO recompile, NO new in-engine gate**). `python/verify_run_kinematics.py` turns the "watch the run" eye-checks into an objective gate (K1 time-monotonic, K2 finite, K3 yaw-rate bounded, K4 speed bounded, **K5 not-spinning-on-spot** = path/cumulative-heading radius, K6 heading-monotonic for turns, **K7 wire-yaw continuity through the ±180° KI-018 zone**, K8 actuator tracking; exit 0/1 for the nightly). `python/build_evidence_pack.py` stands up **D6** — orchestrates the turning-circle/zig-zag/actuator analysers + the health gate into `logs/<run>/evidence_pack/` (`kpis.json` + `EVIDENCE.md` + plots; IMO KPIs auto-selected by controller). `verify_20260620` **8/8** with negative controls (a synthetic pirouette trips K5, NaN+heading-jump trips K2/K3, a wobble trips K6 — not just clean-data passes). Sample pack on the 18-Jun `turning_circle` run: health **8/8**, **Advance 155.3 m (A/Lpp 4.09, IMO PASS), Tactical diameter 158.2 m (DT/Lpp 4.16, IMO PASS)**. **KI-018 log-side corroboration:** that run's plant is healthy and continuous through 271° tracing a clean ~89 m-radius circle ⇒ the spin is **pawn-side visual only** (KI-018 stays IN PROGRESS until the in-engine rerun). Regression on current disk green: Z0 **16/16**, schema-v13 **12/12**, WP-9 **9/9**, re-accept **5/5**, pytest **10/10**. Advances **D1** (sign-confirm tooling) + **D6** (☐→◐). · gates 8/8 auto + 0 new manual (folds into the pending Session-A recompile). · authored via shell (KI-004), AST/py_compile-verified.


- 2026-06-19 · WP-10 · **Canonical listener re-accept — closes KI-017** (pure-Python; **no DTO/schema/C++ change, NO recompile, NO new in-engine gate**). The canonical `python_listener.py` did a **single** `server.accept()` then exited on a client drop, so a UE PIE Stop/Play **ended** the listener (the 14-Jun "re-accept in canonical" claim actually targeted the backup root — KI-017). Refactored `run()` to bind/listen once then loop **accept→serve→re-accept**: the per-session body moved verbatim into a nested `_serve_one_connection` closure (zero arg-threading; invariants intact), `--once` preserves single-shot CI, and **each reconnect is a fresh run** (clock→0, `_sN` log suffix). New `verify_canonical_reaccept.py` (correct workspace-root path): **5/5** (R1 stream, R2 re-accept after a hard RST drop, R3 fresh-run t<0.5 s, R4 2nd reconnect, R5 --once exits); the pre-fix listener scored **1/5**, reproducing the bug. Regression on current disk green: compile-readiness **16/16** (KI-018 `RInterpTo` still intact, B1 22/22), schema-v13 **12/12**, WP-9 **9/9**, pytest **10/10**; MMG+SS4+logging smoke verified across a reconnect (two fresh runs logged). Hardens **WP-3 G4/TC-08** (listener now survives PIE Stop/Play with no relaunch). · gates 5/5 auto + G4 folds into the pending Session-A (no new recompile). · authored via shell (KI-004), py_compile/AST-verified.

- 2026-06-18 · **KI-018 yaw-wrap spin — found in a live run, fix authored** · First post-recompile `turning_circle` PIE run (`unreal-test-run_20260618_201335`) turned cleanly to ~180° then the hull began **spinning on the spot**. Diagnosed from the run log: plant is healthy (yaw rate bounded ~1.1°/s, speed bleeds 2.6→1.5 m/s, no NaN) — the bug is in `NaviSenseShipPawn::Tick`, which smoothed rotation with `FMath::FInterpTo` on the **raw** yaw (actor yaw is [-180,180] but `WireYawToUE` returns [0,360) ⇒ past 180° it chases a 360° gap → spin). **Fix authored:** `FMath::RInterpTo` (shortest-angle, normalized delta rotator); Z0 16/16, braces balanced. **Pending:** Lemuel recompile + rerun to confirm steady circle past 180°. Opened KI-018 (S2), Troubleshooting + Test Log rows. · gates: G7/G_UE7 retest after this recompile.

- 2026-06-18 · **RECOMPILE CONFIRMED (Step 0 cleared)** · Lemuel ran Live Coding on the current tree → **8/8 TUs, "Result: Succeeded"**, patch linked (PID 29432; VS2022 14.44, Win SDK 26100, UE 5.7; ~4.6 s). This is the single recompile that gated the ENTIRE in-engine queue — it compiles the full week's C++ together (WP-4 clock incl. the FTickableGameObject base, WP-5 tests, WP-6 manual, WP-7/8/9 attitude/heave/wave fields, SENSOR-1, ACTUATOR-RIG). Resolves **KI-016** (the unconfirmed-compile integrity gap) and validates **KI-015** end-to-end. The in-PIE gates (G7/G4/G5/C4/G_UE7/8/8+, sensors S1–3, actuator R1–4, WP-6 manual, WP-5 nightly) are now **runnable** — see `Development/work_packets/PENDING_EDITOR_GATES.md`. Tester=Lemuel (screenshot). · No code change this entry — milestone record.

- 2026-06-