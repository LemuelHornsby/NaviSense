# Bug report — KI-___

- **Title:**
- **Date / reporter:**
- **Severity:** S1 Blocker | S2 Major | S3 Minor | S4 Cosmetic
- **Status:** OPEN | IN PROGRESS | RESOLVED | WONTFIX | MONITOR
- **Build / commit:** (Live Coding patch # or VS build / git commit)
- **Area:** Bridge | Controller | Build | Coords | Logging | Git/Backup | Cesium | Performance | Python

## What happened
(observed behavior)

## Expected
(what should happen)

## Steps to reproduce
1.
2. (exact command line, e.g. `python python_listener.py --plant mmg --controller zigzag10 --target unreal -v`)
3.

## Evidence
- Log folder: `logs/<run>/`
- Screenshot / capture: `../Development/Development videos/…`
- Key data: (e.g., `state.csv` shows `mode=approach`, `rudderCmdDeg=0`)

## Root cause
(filled when diagnosed)

## Fix / workaround
(what changed; which file; verification used)

> When resolved, add/move the one-line entry to `../04_Known_Issues_Register.md` with the KI-ID.
