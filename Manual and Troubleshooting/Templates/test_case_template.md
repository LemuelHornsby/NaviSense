# Test case — TC-___

- **Title:**
- **Level:** L0 Protocol | L1 Smoke | L2 Correctness | L3 KPIs | L4 Regression | L5 Acceptance
- **Preconditions:** (venv active; `NaviSense_Monaco` open; build = …)

## Procedure
1.
2.
3.

## Pass criteria
- (objective, log-based where possible — e.g., "`rudderCmdDeg` reverses ≥2×; bow swings starboard for +rudder")

## Evidence to capture
- `logs/<run>/` ; analysis plot ; screenshot

## Result
- Date / build / tester / PASS|FAIL|IN PROGRESS → record in `../05_Test_Log.md`
